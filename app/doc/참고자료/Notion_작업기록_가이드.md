# Notion 작업 기록 가이드

**작성일**: 2026-06-29

메인 에이전트가 사용자 명령으로 수행한 작업을 Notion에 일관되게 기록하기 위한 규칙 문서.

---

## 1. 목적

- 사용자가 내린 명령·작업 이력을 Notion에서 캘린더로 관리한다.
- 기록 방식을 문서로 고정하여 매 작업마다 **동일한 구조**로 정리되도록 한다.

---

## 2. 저장 위치

| 항목 | 값 |
|------|-----|
| 워크스페이스 | 사용자 개인 Notion (강륭강 / fbdrkd4858@gmail.com) |
| 최상위 | `회사` 페이지 |
| 기록 폴더 | `회사 > 📱 PDA` (id `38ee607c-6ab8-818d-9fe7-e5ab7d481533`) |
| 작업 DB | `작업 기록` (database id `268d91e8-8439-48ff-8489-509cf7e2f699`) |
| 데이터소스 | `collection://6132122f-172a-4481-8f13-9d3d353ce515` |
| 기본 뷰 | 캘린더 (`날짜` 기준) |

> ⚠️ 회사 PDA 워크스페이스가 아닌 **사용자 개인 Notion**임에 유의.

---

## 3. 구조 — 캘린더 데이터베이스

작업 1건 = DB의 1행(row). 캘린더 뷰에서 날짜 칸에 작업이 표시된다.

```
회사
└── 📱 PDA
    └── 🗓️ 작업 기록 (DB, 캘린더 뷰)
        ├── [작업1]  날짜·분류·상태·요약  + 본문(상세)
        ├── [작업2]  ...
        └── ...
```

### 3.1 행(row) 속성

| 속성 | 타입 | 설명 |
|------|------|------|
| 작업명 | Title | 작업 제목 |
| 날짜 | Date | 작업 수행일 (캘린더 기준). 생성 시 `date:날짜:start` 키 사용 |
| 분류 | Select | `코드수정` / `문서작성` / `오류처리` / `테스트` / `기타` |
| 상태 | Status | `시작 전` / `진행 중` / `완료` |
| 요약 | Text | 작업 요약 1~2줄 |

### 3.2 행 페이지 본문 (상세)

- `## 명령` — 사용자 명령 원문 (인용)
- `## 결과` — 수행 결과 (수정 파일/내용, 검증 결과 등)
- `## 비고` — 특이사항·주의점 (선택)

---

## 4. 기록 기준

| 구분 | 기록 여부 |
|------|:--------:|
| 실제 작업 (코드 수정·문서 작성·오류 처리·테스트 등) | ✅ 기록 |
| 단순 질문·확인·상태 조회 | ❌ 제외 |

- 사용자가 "노션에 기록해줘"라고 명시하면 위 기준과 무관하게 기록한다.

---

## 5. 동작 흐름

1. 실제 작업 수행
2. `작업 기록` DB(데이터소스 `collection://6132122f-...`)에 행 추가
   - 작업명 / `date:날짜:start` / 분류 / 상태 / 요약 입력
3. 행 페이지 본문에 상세(`## 명령` / `## 결과` / `## 비고`) 작성

---

## 6. 한계

- 시스템 자동 트리거가 아니라 메인 에이전트가 작업 흐름에서 챙기는 방식이다.
  (hook은 셸 명령만 실행 가능, Notion MCP 기록 불가)
- 기록 누락 시 사용자가 "노션에 기록해줘" 한마디로 보완한다.
- 현재 MCP 도구에는 휴지통 이동 기능이 없어, 페이지 삭제는 Notion 화면에서 직접 한다.

---

## 7. 일정 관리 (회사 폴더)

작업 기록과 별개로, **계획(일정)** 은 `회사 > 일정` 캘린더 DB로 관리한다.

| 항목 | 값 |
|------|-----|
| 위치 | `회사` 페이지 직속 (PDA 폴더 아님) |
| 일정 DB | `일정` (database id `7967279c-6c0c-4135-baab-4d61d8a417be`) |
| 데이터소스 | `collection://63d103fb-72d3-40cf-a423-5bb72c4e63a0` |

### 7.1 행 속성

| 속성 | 타입 | 설명 |
|------|------|------|
| 일정명 | Title | 할 일 제목 |
| 기간 | Date | 예정일 (생성 시 `date:기간:start` 키 사용) |
| 구분 | Select | `개발` / `문서` / `테스트` / `회의` / `기타` |
| 완료 | Checkbox | 미체크 → 수행 완료 시 체크 (`__YES__`) |
| 메모 | Text | 비고 |

### 7.2 운영 흐름

1. **아침 등록**: 사용자가 "오늘 ~작업 하겠다"고 하면 → 일정 DB에 오늘 날짜(`기간`)로 항목 추가, `완료` = 미체크.
2. **작업 수행 시**: 해당 작업을 실제로 처리하면 → 그 일정 항목의 `완료` 체크박스를 체크(`완료`=`__YES__`) + 작업 기록 DB에도 1행 기록.
3. 일정명과 작업명을 맞춰 두면 어떤 일정이 어떤 작업으로 완료됐는지 대조하기 쉽다.

---

## 관련 문서

- 메모리: `feedback_notion_worklog` — 동일 규칙의 메모리 요약본
- 메모리: `project_notion_mcp` — Notion MCP 연결 정보
