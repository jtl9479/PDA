# 창고별 기능 분기

**작성일**: 2026-01-15

---

## 1. 창고 목록

| 창고명 | 창고코드 |
|--------|----------|
| 이천1센터 | 4001 |
| 삼일냉장 | IN10273 |
| SWC | IN60464 |
| 부산2 | 4004 |
| 탑로지스 | IN63279 |

---

## 2. searchType별 창고 조건 사용 여부

| searchType | 기능 | 창고 조건 (WHERE) | 비고 |
|:----------:|------|:-----------------:|------|
| 0 | 이마트 출하 | ✅ 사용 | 선택한 창고 데이터만 조회 |
| 1 | 생산 계근 | ❌ 미사용 | 창고 무관, 모든 생산 데이터 조회 |
| 2 | 홈플러스 출하 | ✅ 사용 | 선택한 창고 데이터만 조회 |
| 3 | 도매업체 출하 | ✅ 사용 | 선택한 창고 데이터만 조회 |
| 4 | 비정량 출하 | ❌ 미사용 | 창고 무관 |
| 5 | 홈플러스 비정량 | ❌ 미사용 | 창고 무관 |
| 6 | 롯데 출하 | ✅ 사용 | 선택한 창고 데이터만 조회 |
| 7 | 생산 라벨 | ❌ 미사용 | 창고 무관, 모든 생산 데이터 조회 |

**주의**: 앱에서 창고별 기능 제한 로직 **없음**. 모든 버튼 항상 활성화.

---

## 3. GR_WAREHOUSE_CODE 사용 방식

**서버 요청 시 WHERE 조건으로 전송** (ProgressDlgShipSearch.java Line 117-196):

```java
// 앱에서 선택한 창고에 해당하는 데이터만 조회
if(Common.selectWarehouse.equals("삼일냉장")){
    data += " AND GR_WAREHOUSE_CODE = 'IN10273'";
}else if(Common.selectWarehouse.equals("SWC")){
    data += " AND GR_WAREHOUSE_CODE = 'IN60464'";
}else if(Common.selectWarehouse.equals("이천1센터")){
    data += " AND GR_WAREHOUSE_CODE = '4001'";
}else if(Common.selectWarehouse.equals("부산센터")){
    data += " AND GR_WAREHOUSE_CODE = '4004'";
}else if(Common.selectWarehouse.equals("탑로지스")){
    data += " AND GR_WAREHOUSE_CODE = 'IN63279'";
}
```

---

## 4. 창고 선택 흐름

```
LoginActivity.java:101
    └─ Common.selectWarehouse = "이천1센터" (스피너 선택)
           │
           ▼
MainActivity.java
    └─ 사용자가 기능 버튼 클릭
           │
           ▼
ProgressDlgShipSearch.java:117-196
    └─ searchType에 따라 창고 조건 추가
           │
           ▼
서버 VIEW에서 GR_WAREHOUSE_CODE 필터링
```

---

## 5. 관련 파일

| 파일 | 역할 |
|------|------|
| LoginActivity.java | 창고 선택 (스피너) |
| MainActivity.java | searchType 설정, 기능 버튼 |
| ProgressDlgShipSearch.java | 창고 조건 WHERE절 생성 |
| Common.java | selectWarehouse 전역 변수 |

---

**최종 수정일**: 2026-01-15
