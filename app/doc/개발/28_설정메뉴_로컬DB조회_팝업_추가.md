# 설정 메뉴에 로컬DB 조회 팝업 추가

**작성일**: 2026-04-07
**목적**: 프린터설정 메뉴 클릭 시 4개 항목(프린터설정, 출하대상, 바코드정보, 계근데이터) 선택 다이얼로그 표시

---

## AI 제약 조건

- 기존 WHERE 조건, 로직을 임의로 제거/추가/변경하지 않는다
- 문서에 명시된 step만 진행하고, 다음 step은 지시를 기다린다
- step 완료 후 체크리스트 + 진행 현황을 반드시 업데이트한다
- 문서에 없는 개선/리팩토링을 임의로 수행하지 않는다
- 기존 기능과 100% 동일하게 동작해야 한다

---

## 1. 현재 구조

### menu/main.xml

```xml
<item android:id="@+id/action_pinrtsettings" android:title="프린터설정" showAsAction="never"/>
<item android:id="@+id/action_daysettings" android:title="날짜설정" showAsAction="always|withText"/>
<item android:id="@+id/action_shipmentlist" android:title="출하대상" showAsAction="always|withText"/>
```

### MainActivity.onOptionsItemSelected()

- `action_pinrtsettings` → SettingActivity 바로 이동
- `action_daysettings` → DatePickerDialog
- `action_shipmentlist` → showShipmentListDialog()

### 문제점

- TB_BARCODE_INFO, TB_GOODS_WET 데이터 확인 방법 없음
- 디버깅/테스트 시 로컬 DB 상태 파악 불가

---

## 2. 변경 구조

### 변경 후 동작

```
프린터설정 메뉴 클릭 (점 3개 → 프린터설정)
    ↓
선택 다이얼로그 표시:
  1. 프린터설정 → SettingActivity 이동 (기존 동작)
  2. 출하대상 → showShipmentListDialog() (기존 개발24)
  3. 바코드정보 → showBarcodeInfoDialog() (신규)
  4. 계근데이터 → showGoodsWetDialog() (신규)
```

### 팝업 컬럼 정의

**바코드정보 (TB_BARCODE_INFO):**

| # | 컬럼 | getter | 비고 |
|:-:|------|--------|------|
| 1 | PPCODE | getPACKER_PRODUCT_CODE() | 패커상품코드 |
| 2 | 품목명 | getITEM_NAME_KR() | 한글 상품명 |
| 3 | 바코드상품코드 | getBARCODEGOODS() | |

**계근데이터 (TB_GOODS_WET):**

| # | 컬럼 | getter | 비고 |
|:-:|------|--------|------|
| 1 | GI_D_ID | getGI_D_ID() | 출고상세번호 |
| 2 | 중량 | getWEIGHT() | |
| 3 | 바코드 | getBARCODE() | |
| 4 | SAVE_TYPE | getSAVE_TYPE() | F=미전송, Y=전송완료 |

### ActionBar 메뉴 변경

- `action_shipmentlist` 메뉴 제거 (선택 다이얼로그에 통합)
- `action_pinrtsettings` 클릭 시 선택 다이얼로그 표시

---

## 3. 수정 대상 파일

| # | 파일 | 위치 | 수정 내용 |
|:-:|------|------|----------|
| 1 | **menu/main.xml** | action_shipmentlist | 제거 (다이얼로그에 통합) |
| 2 | **DBHandler.java** | 신규 메서드 2개 | selectqueryBarcodeInfoForPopup(), selectqueryGoodsWetForPopup() |
| 3 | **MainActivity.java** | onOptionsItemSelected, 신규 메서드 3개 | showSettingMenuDialog(), showBarcodeInfoDialog(), showGoodsWetDialog() |

---

## 4. 수정 상세

### 4.1 menu/main.xml

**변경 후:**

```xml
<item
    android:id="@+id/action_pinrtsettings"
    android:orderInCategory="100"
    android:title="설정"
    app:showAsAction="never"/>

<item
    android:id="@+id/action_daysettings"
    android:orderInCategory="100"
    android:title="날짜설정"
    app:showAsAction="always|withText"/>
```

- `action_shipmentlist` 제거
- `action_pinrtsettings` title: "프린터설정" → "설정"

### 4.2 DBHandler.java - 신규 메서드 2개

**selectqueryBarcodeInfoForPopup():**

```java
public static ArrayList<Barcodes_Info> selectqueryBarcodeInfoForPopup(Context context) {
    ArrayList<Barcodes_Info> list_bi = new ArrayList<Barcodes_Info>();
    DBHelper mDbHelper = new DBHelper(context);
    mDbHelper.open();
    try {
        Cursor cursor;
        String sqlStr = "SELECT "
                + DBInfo.PACKER_PRODUCT_CODE + ", "
                + DBInfo.ITEM_NAME_KR + ", "
                + DBInfo.BARCODEGOODS
                + " FROM " + DBInfo.TABLE_NAME_BARCODE_INFO
                + " ORDER BY " + DBInfo.PACKER_PRODUCT_CODE + " ASC";
        cursor = mDbHelper.selectSql(sqlStr);
        Barcodes_Info bi;
        while (cursor.moveToNext()) {
            bi = new Barcodes_Info();
            bi.setPACKER_PRODUCT_CODE(Common.nullCheck(cursor.getString(cursor.getColumnIndex("PACKER_PRODUCT_CODE")), ""));
            bi.setITEM_NAME_KR(Common.nullCheck(cursor.getString(cursor.getColumnIndex("ITEM_NAME_KR")), ""));
            bi.setBARCODEGOODS(Common.nullCheck(cursor.getString(cursor.getColumnIndex("BARCODEGOODS")), ""));
            list_bi.add(bi);
        }
        cursor.close();
    } catch (Exception e) {
        if (Common.D) {
            Log.v(TAG, "selectqueryBarcodeInfoForPopup exception -> " + e.getMessage().toString());
        }
    }
    mDbHelper.close();
    return list_bi;
}
```

**selectqueryGoodsWetForPopup():**

```java
public static ArrayList<Goodswets_Info> selectqueryGoodsWetForPopup(Context context) {
    ArrayList<Goodswets_Info> list_gi = new ArrayList<Goodswets_Info>();
    DBHelper mDbHelper = new DBHelper(context);
    mDbHelper.open();
    try {
        Cursor cursor;
        String sqlStr = "SELECT "
                + DBInfo.GI_D_ID + ", "
                + DBInfo.WEIGHT + ", "
                + DBInfo.BARCODE + ", "
                + DBInfo.SAVE_TYPE
                + " FROM " + DBInfo.TABLE_NAME_GOODS_WET
                + " ORDER BY " + DBInfo.GI_D_ID + " ASC";
        cursor = mDbHelper.selectSql(sqlStr);
        Goodswets_Info gi;
        while (cursor.moveToNext()) {
            gi = new Goodswets_Info();
            gi.setGI_D_ID(Common.nullCheck(cursor.getString(cursor.getColumnIndex("GI_D_ID")), ""));
            gi.setWEIGHT(Common.nullCheck(cursor.getString(cursor.getColumnIndex("WEIGHT")), ""));
            gi.setBARCODE(Common.nullCheck(cursor.getString(cursor.getColumnIndex("BARCODE")), ""));
            gi.setSAVE_TYPE(Common.nullCheck(cursor.getString(cursor.getColumnIndex("SAVE_TYPE")), ""));
            list_gi.add(gi);
        }
        cursor.close();
    } catch (Exception e) {
        if (Common.D) {
            Log.v(TAG, "selectqueryGoodsWetForPopup exception -> " + e.getMessage().toString());
        }
    }
    mDbHelper.close();
    return list_gi;
}
```

### 4.3 MainActivity.java

**onOptionsItemSelected() 변경:**

```java
if (id == R.id.action_pinrtsettings) {
    showSettingMenuDialog();
    return true;
}
```

- `action_shipmentlist` 분기 제거 (다이얼로그에 통합)

**showSettingMenuDialog() 신규:**

```java
private void showSettingMenuDialog() {
    String[] items = {"프린터설정", "출하대상", "바코드정보", "계근데이터"};
    new AlertDialog.Builder(this)
        .setTitle("설정")
        .setItems(items, (dialog, which) -> {
            switch (which) {
                case 0:
                    startActivity(new Intent(MainActivity.this, SettingActivity.class));
                    break;
                case 1:
                    showShipmentListDialog();
                    break;
                case 2:
                    showBarcodeInfoDialog();
                    break;
                case 3:
                    showGoodsWetDialog();
                    break;
            }
        })
        .setNegativeButton("닫기", null)
        .show();
}
```

**showBarcodeInfoDialog() 신규:**

```java
private void showBarcodeInfoDialog() {
    ArrayList<Barcodes_Info> list = DBHandler.selectqueryBarcodeInfoForPopup(this);
    if (list.size() == 0) {
        Toast.makeText(this, "바코드정보가 없습니다.", Toast.LENGTH_SHORT).show();
        return;
    }
    StringBuilder sb = new StringBuilder();
    for (Barcodes_Info bi : list) {
        sb.append("[" + bi.getPACKER_PRODUCT_CODE() + "] " + bi.getITEM_NAME_KR() + "\n");
        sb.append("  바코드: " + bi.getBARCODEGOODS() + "\n");
        sb.append("─────────────────────────\n");
    }
    new AlertDialog.Builder(this)
        .setTitle("바코드정보 (" + list.size() + "건)")
        .setMessage(sb.toString())
        .setPositiveButton("닫기", null)
        .show();
}
```

**showGoodsWetDialog() 신규:**

```java
private void showGoodsWetDialog() {
    ArrayList<Goodswets_Info> list = DBHandler.selectqueryGoodsWetForPopup(this);
    if (list.size() == 0) {
        Toast.makeText(this, "계근데이터가 없습니다.", Toast.LENGTH_SHORT).show();
        return;
    }
    StringBuilder sb = new StringBuilder();
    for (Goodswets_Info gi : list) {
        sb.append("[" + gi.getGI_D_ID() + "] 중량:" + gi.getWEIGHT() + "\n");
        sb.append("  바코드:" + gi.getBARCODE() + "\n");
        sb.append("  전송:" + gi.getSAVE_TYPE() + "\n");
        sb.append("─────────────────────────\n");
    }
    new AlertDialog.Builder(this)
        .setTitle("계근데이터 (" + list.size() + "건)")
        .setMessage(sb.toString())
        .setPositiveButton("닫기", null)
        .show();
}
```

---

## 5. 사이드이펙트

- 기존 프린터설정 기능 유지 (다이얼로그에서 선택 시 SettingActivity 이동)
- 기존 출하대상 팝업 기능 유지 (다이얼로그에서 선택)
- action_shipmentlist 메뉴 제거되지만 기능은 다이얼로그에 통합
- import 추가 필요: Barcodes_Info, Goodswets_Info (이미 import 되어있는지 확인 필요)

---

## 6. 개발 플랜

### Step 1: DBHandler 신규 메서드 2개 추가

**Part 1. 분석**
- 대상: DBHandler.java
- 범위: selectqueryBarcodeInfoForPopup(), selectqueryGoodsWetForPopup() 추가
- 주의할 점: 기존 메서드 변경 없음

**체크리스트**
- [x] Part 1: 분석 완료
- [x] Part 2: 변환 수행
- [x] Part 3: 컴파일 확인 (BUILD SUCCESSFUL)

**Part 6. 변경 내용**:
- **무엇을**: DBHandler.java에 selectqueryBarcodeInfoForPopup(), selectqueryGoodsWetForPopup() 2개 메서드 추가
- **왜**: TB_BARCODE_INFO, TB_GOODS_WET 로컬 데이터 확인 수단 없음
- **어떻게**: selectqueryShipmentForPopup() 뒤에 신규 메서드 2개 추가

---

### Step 2: menu/main.xml 변경 + MainActivity 다이얼로그 구현

**Part 1. 분석**
- 대상: menu/main.xml, MainActivity.java
- 범위: action_shipmentlist 제거, action_pinrtsettings → 선택 다이얼로그, 신규 메서드 3개
- 주의할 점: import 확인 (Barcodes_Info, Goodswets_Info)

**체크리스트**
- [x] Part 1: import 확인 (Barcodes_Info, Goodswets_Info 추가)
- [x] Part 2: 변환 수행
- [x] Part 3: 컴파일 확인 (BUILD SUCCESSFUL)
- [ ] Part 4: 설정 → 4개 항목 선택 → 각 팝업 정상 표시 확인

**Part 6. 변경 내용**:
- **무엇을**: menu/main.xml에서 action_shipmentlist 제거, action_pinrtsettings → 선택 다이얼로그, 신규 메서드 3개 추가
- **왜**: 로컬 DB 3개 테이블 데이터 확인 기능을 설정 메뉴에 통합
- **어떻게**: showSettingMenuDialog() 4개 항목 선택 → 프린터설정/출하대상/바코드정보/계근데이터

---

### 개발 순서 요약

```
Step 1: DBHandler 신규 메서드 2개 추가
    ↓
Step 2: menu/main.xml 변경 + MainActivity 다이얼로그 구현
```

---

## 7. 진행 현황

| Step | 작업 | 상태 |
|------|------|------|
| 1 | DBHandler 신규 메서드 2개 추가 | ✅ 완료 |
| 2 | menu/main.xml 변경 + MainActivity 다이얼로그 구현 | ✅ 완료 (실기기 테스트 대기) |

---

## 관련 문서

- `app/doc/개발/24_출하대상목록_팝업_추가.md`
- `app/doc/소스분석/35_로컬DB_구조.md`

---

**문서 버전**: 1.0
