# 이마트 출하계근 AI 정적 검증 가이드

**작성일**: 2026-06-29
**목적**: `app/doc/테스트/03_이마트_출하계근_전체테스트.html` 의 23개 테스트 시나리오 (정량 12 + 비정량 11)를 AI가 코드 기반 정적 검증으로 수행하기 위한 계획 및 검증 매핑 문서. 코드 변경은 없으며 검증만 진행한다.

---

## AI 제약 조건

- 기존 WHERE 조건, 로직을 임의로 제거/추가/변경하지 않는다
- 문서에 명시된 step만 진행하고, 다음 step은 지시를 기다린다
- step 완료 후 체크리스트 + 진행 현황을 반드시 업데이트한다
- 문서에 없는 개선/리팩토링을 임의로 수행하지 않는다
- 기존 기능과 100% 동일하게 동작해야 한다

---

## 1. 현재 구조

### 검증 대상 시나리오 개요

- 정량(searchType 0): 시나리오 1 ~ 12 (총 12개)
- 비정량(searchType 4): 비정량 1 ~ 11 (총 11개)
- **합계**: 23개 시나리오 (요구사항에서 20개로 언급되었으나 실제 HTML 기준 23개)

### 대상 소스 파일

| 파일 | 경로 | 역할 |
|------|------|------|
| BixolonShipmentActivity.java | app/src/main/java/.../highland_emart/ | 계근 화면 핵심 로직 |
| ProgressDlgShipSearch.java | app/src/main/java/.../common/ | 출하대상 서버 조회 |
| ProgressDlgBarcodeSearch.java | app/src/main/java/.../common/ | 바코드정보 서버 조회 |
| ProgressDlgGoodsWetSearch.java | app/src/main/java/.../common/ | 계근 데이터 재조회 |
| LabelPrintHelper.java | app/src/main/java/.../print/ | 라벨 인쇄 분기 |
| DBHandler.java | app/src/main/java/.../db/ | 로컬 SQLite 처리 |
| search_production_nonfixed.jsp | Tomcat/webapps/ROOT/inno/ | 비정량 출하대상 쿼리 |
| search_barcode_info_nonfixed.jsp | Tomcat/webapps/ROOT/inno/ | 비정량 바코드정보 쿼리 |

### 문제점

- 본 작업에서 수정할 코드는 없음
- 목적: 수동 디바이스 조작 없이 AI가 코드·쿼리를 읽어 예상 동작을 정적으로 검증
- 정적 검증으로 확인 불가한 항목(라벨 출력 육안 확인, 블루투스 실연결, 저울 연동 등)을 명확히 구분

---

## 2. 변경 구조

### 검증 흐름

```
[HTML 시나리오 원본 읽기]
    ↓
[각 시나리오의 확인 항목 추출]
    ↓
[AI 정적 검증 가능 여부 판단]
    ├── 정적 검증 가능 → 해당 소스/메서드/쿼리 코드 확인
    └── 디바이스 실측 필요 → 항목만 명시, 검증 보류
    ↓
[시나리오별 검증 결과 문서화]
    ↓
[통합 정리]
```

---

## 3. 수정 대상 파일

| # | 파일 | 위치 | 수정 내용 |
|:-:|------|------|----------|
| - | **해당 없음** | - | 이 가이드는 코드 변경 없는 검증 전용 문서 |

> 원본 100% 동일 원칙에 따라 이 작업은 검증만이며 코드 변경을 수행하지 않는다.

---

## 4. 정적 검증 vs 디바이스 실측 분류표

### 4.1 정량 시나리오 (searchType 0)

| 시나리오 | 확인 항목 | AI 정적 검증 | 디바이스 실측 | 검증 소스/메서드 |
|---------|---------|:---:|:---:|------|
| 1-1 | 출하대상받기 → 목록 구성 | O | - | ProgressDlgShipSearch.doInBackground() → URL_SEARCH_PRODUCTION |
| 1-2 | 바코드정보 없는 상품 안내 다이얼로그 | O | - | ProgressDlgBarcodeSearch.onPostExecute() 빈 목록 분기 |
| 1-3 | 계근 화면 진입 / 센터·BL·지점 스피너 | O(조건) | O(UI) | BixolonShipmentActivity.onCreate() 레이아웃 분기 |
| 1-4 | 바코드 스캔 → 상품명·중량 표시 | - | O | 하드웨어 스캔 필요 |
| 1-5 | 중량 반올림 표시 | O | - | BixolonShipmentActivity:1661 `if(SEARCH_TYPE_EMART)` → `Math.round(*100.0)/100.0` |
| 1-6 | 라벨 자동 출력 | - | O | 블루투스 프린터 실연결 필요 |
| 1-7 | 계근 전송 버튼 → 완료 안내 | O(패킷) | O(서버) | GoodsWetSendTask.doInBackground() 패킷 구조 |
| 1-8 | 계근완료 상태 표시 | O | - | updatequeryShipment() 호출 후 어댑터 갱신 |
| 2-1~2 | 첫/두 번째 바코드 스캔·중량 표시 | - | O | 하드웨어 스캔 필요 |
| 2-3 | 계근 수량 1/N 표시 | O | - | DetailAdapter에서 BOX_CNT/REQUEST_QTY 비교 로직 |
| 2-4 | 동일 바코드 재스캔 → 중복 차단 | O | - | BixolonShipmentActivity:1185-1193 `duplicatequeryGoodsWet_check()` → Toast "이미 스캔한 바코드입니다" |
| 2-5 | 두 번째 스캔 후 수량 2/N | - | O | 하드웨어 스캔 필요 |
| 2-6 | 센터 합계 실시간 갱신 | O | - | BixolonShipmentActivity:1662 `centerWorkWeight = Math.round(*100.0)/100.0` |
| 2-7 | 전체 완료 감지 다이얼로그 | O | - | MESSAGE_COMPLETE Handler 분기 |
| 3-1 | 수기 모드 선택 | - | O | UI 조작 필요 |
| 3-2 | 중량 수동 입력 | - | O | UI 조작 필요 |
| 3-3 | 수기 입력 중량 반올림 | O | - | BixolonShipmentActivity:660 `String.format("%.1f", weight_double)` + :1661 round 처리 |
| 3-4 | 계근 처리 → 수량/중량 반영 | O(논리) | O(UI) | setGI_QTY() `Math.round((*+weight_double)*10.0)/10.0` |
| 3-5 | 라벨 출력 | - | O | 프린터 실연결 필요 |
| 3-6 | 계근 전송 | O(패킷) | O(서버) | GoodsWetSendTask |
| 4-1 | 소비기한 입력창 자동 표시 (수기 한정) | O | - | BixolonShipmentActivity:678-702 KILKOY+MEAT_CENTER_STORE → ExpiryEnterActivity Intent |
| 4-2 | 소비기한 입력 UI | - | O | Activity 화면 필요 |
| 4-3 | 날짜 입력 허용/방어 | O(조건) | O(UI) | ExpiryEnterActivity 입력 검증 로직 |
| 4-4 | 확인 후 계근 화면 복귀 | O | - | onActivityResult(GET_DATA_REQUEST) 분기 |
| 4-5 | 라벨 소비기한 출력 | - | O | 프린터 실연결 필요 |
| 4-6 | 소비기한 누락 → "소비기한정보 필수" 차단 | O | - | BixolonShipmentActivity:1266-1275 Toast 메시지 |
| 4-7 | 소비기한 포함 전송 | O(패킷) | O(서버) | packet에 소비기한 포함 여부 |
| 5-1 | 스캔 중량 반올림 표시 | O | - | :1661 searchType 0 분기 → `Math.round(*100.0)/100.0` |
| 5-2 | 수기 입력 반올림 표시 | O | - | :660 `format("%.1f")` |
| 5-3 | 라벨 중량 반올림 | - | O | 프린터 실연결 필요 |
| 5-4 | 비정량과 반올림 차이 | O | - | searchType 비교 분기 확인 |
| 6-1 | 라벨 자동 인쇄 ON 동작 | - | O | 프린터 실연결 필요 |
| 6-2 | 이마트 양식 라벨 | - | O | 프린터 실연결 필요 |
| 6-3~5 | 라벨 내용 (상품명/중량/바코드) | - | O | 육안 확인 필요 |
| 6-6 | 메뉴 재출력 기능 | O | O | MESSAGE_REPRINT Handler 존재 여부 |
| 6-7 | 자동 인쇄 OFF 동작 | O(논리) | O(UI) | 자동 인쇄 스위치 분기 코드 |
| 6-8 | 합계 라벨 인쇄 | O(논리) | O(UI) | 합계 버튼 클릭 핸들러 |
| 7-1 | 전송 버튼 → 전송 진행 | O | - | GoodsWetSendTask AsyncTask 기동 |
| 7-2 | 전송 완료 안내 | O | - | onPostExecute() Toast/Dialog 분기 |
| 7-3 | 실패 시 오류 안내 | O | - | onPostExecute() 실패 분기 |
| 7-4 | 계근완료 상태 갱신 | O | - | updatequeryShipment() → arSM 어댑터 갱신 |
| 8-1 | 빈 목록 표시 | O | - | ProgressDlgShipSearch.onPostExecute() 빈 결과 처리 |
| 8-2 | 사용자 안내 | O | - | 빈 목록 안내 Toast/Dialog 코드 |
| 9-1 | 연결 실패 안내 | O | - | HttpHelper.sendDataDb() 예외 → onPostExecute() 오류 분기 |
| 9-2 | 화면 유지 | O | - | try-catch 처리 후 화면 종료 방지 |
| 9-3 | 앱 비정상 종료 없음 | O | - | 예외 처리 코드 존재 여부 |
| 10-1 | 재조회 목록 갱신 | O | - | ProgressDlgShipSearch 재호출 시 DB 초기화 로직 |
| 10-2 | 추가/삭제 반영 | O | - | deleteAll + insertAll 패턴 확인 |
| 10-3 | 기존 계근 데이터 유지 | O | - | TB_GOODS_WET 테이블 재조회 시 유지 여부 |
| 11-1 | 특정 창고 조회 | O | - | ProgressDlgShipSearch 파라미터에 WH_CODE 포함 여부 |
| 11-2 | 창고 변경 후 재조회 | O | - | 동일 파라미터 처리 |
| 12-1 | 개별 삭제 다이얼로그 | O | - | 삭제 버튼 핸들러 AlertDialog 코드 |
| 12-2 | 삭제 처리 | O | - | DBHandler.deleteGoodsWet() 존재 여부 |
| 12-3 | 계근수량 N-1 갱신 | O | - | 삭제 후 listGoodsWet 재로드 |
| 12-4 | 재스캔 가능 | O | - | duplicatequeryGoodsWet 삭제 후 조회 결과 없음 |
| 12-5 | 계근완료 상태 해제 | O | - | updatequeryShipment() 취소 로직 |

### 4.2 비정량 시나리오 (searchType 4)

| 시나리오 | 확인 항목 | AI 정적 검증 | 디바이스 실측 | 검증 소스/메서드 |
|---------|---------|:---:|:---:|------|
| 비정량1-1 | 출하대상받기 → 목록 구성 | O | - | ProgressDlgShipSearch:151 `searchType.equals("4")` → URL_SEARCH_PRODUCTION_NONFIXED |
| 비정량1-2 | 바코드정보 없는 상품 안내 | O | - | ProgressDlgBarcodeSearch.onPostExecute() 빈 목록 분기 |
| 비정량1-3 | 계근 화면 진입 | O(조건) | O(UI) | MainActivity.startWeighing(SEARCH_TYPE_NONFIXED) |
| 비정량1-4 | 스캔 시 중량 반올림 없이 그대로 표시 | O | - | BixolonShipmentActivity:1661 `if(SEARCH_TYPE_EMART)` → 4는 해당 안 됨 → 반올림 미적용 |
| 비정량1-5 | 비정량(M8/M9) 양식 라벨 자동 출력 | O(분기) | O(출력) | BixolonShipmentActivity:1700-1701 `SEARCH_TYPE_NONFIXED` 분기 → LabelPrintHelper M8/M9 case |
| 비정량1-6 | 계근 전송 | O(패킷) | O(서버) | GoodsWetSendTask searchType 4 분기 |
| 비정량1-7 | 계근완료 표시 | O | - | updatequeryShipment() |
| 비정량2-1 | M8/M9 외 품목도 조회됨 | O | - | search_production_nonfixed.jsp:129 `AND BARCODE_TYPE IN ('M8','M9')` 주석처리 확인 (2026-06-23 적용) |
| 비정량2-2 | 기존 M8/M9 품목 유지 | O | - | 동일 JSP SELECT에 BARCODE_TYPE 컬럼 포함 |
| 비정량2-3 | 스캔/계근 정상 | - | O | 하드웨어 스캔 필요 |
| 비정량3-1 | 첫 스캔 계근수량 1 증가 | - | O | 하드웨어 스캔 필요 |
| 비정량3-2 | 동일 바코드 재스캔 허용 (경고 없음) | O | - | BixolonShipmentActivity:1187 `if(SEARCH_TYPE_NONFIXED || SEARCH_TYPE_HOMEPLUS_NONFIXED)` → 중복 체크 건너뜀 |
| 비정량3-3 | 계근수량 누적 (2, 3, ...) | O | - | 동일 바코드 insertGoodsWet() 반복 허용 논리 |
| 비정량3-4 | 스캔마다 중량 합계 반영 | O | - | setGI_QTY() 누적 로직 |
| 비정량3-5 | 정량과 차이 (비정량만 반복 허용) | O | - | 분기 조건 비교 |
| 비정량4-1 | 비정량 바코드 매칭 | O | - | ProgressDlgBarcodeSearch:82 `searchType.equals("4")` → URL_SEARCH_BARCODE_INFO_NONFIXED |
| 비정량4-2 | 상품정보 표시 | - | O | UI 표시 확인 필요 |
| 비정량4-3 | 중량 그대로 표시 | O | - | searchType 4는 round 분기 미해당 |
| 비정량5-1 | 수기 모드 선택 | - | O | UI 조작 필요 |
| 비정량5-2 | 중량 수동 입력 | - | O | UI 조작 필요 |
| 비정량5-3 | 수기 입력 중량 그대로 표시 (반올림 없음) | O | - | :1661 searchType 0만 반올림 → 4는 미적용 |
| 비정량5-4 | 계근 처리 | O(논리) | O(UI) | insertGoodsWet() + setGI_QTY() |
| 비정량5-5 | 비정량(M8/M9) 양식 라벨 | O(분기) | O(출력) | LabelPrintHelper case "M9", case "M8" |
| 비정량5-6 | 계근 전송 | O(패킷) | O(서버) | GoodsWetSendTask |
| 비정량6-1 | M8/M9 양식 출력 | O(분기) | O(출력) | LabelPrintHelper:648 case "M9" / :663 case "M8" 분기 존재 |
| 비정량6-2 | 물류코드 표기 정상 | - | O | 라벨 육안 확인 필요 |
| 비정량6-3 | 라벨 상품명/중량/바코드 | - | O | 라벨 육안 확인 필요 |
| 비정량6-4 | 물류코드 빈값 시 오류 | O(조건) | - | LabelPrintHelper 물류코드 파라미터 null/empty 처리 확인 |
| 비정량6-5 | ERP 물류코드 입력 후 해결 | - | O | 실 ERP 데이터 조작 필요 |
| 비정량7-1 | 복수 건 전송 | O | - | GoodsWetSendTask list_send_info 루프 처리 |
| 비정량7-2 | 전송 완료 안내 | O | - | onPostExecute() 성공 Toast/Dialog |
| 비정량7-3 | 모든 건 계근완료 갱신 | O | - | updatequeryShipment() 루프 |
| 비정량8-1 | 계근 전송 | O(패킷) | O(서버) | GoodsWetSendTask |
| 비정량8-2 | 계근완료 반영 | O | - | updatequeryShipment() |
| 비정량8-3 | 재조회 시 유지 | O | - | TB_GOODS_WET 독립 저장 구조 |
| 비정량9-1 | 빈 목록 표시 | O | - | ProgressDlgShipSearch.onPostExecute() 빈 결과 처리 |
| 비정량9-2 | 사용자 안내 | O | - | 안내 코드 존재 여부 |
| 비정량10-1 | 연결 실패 안내 | O | - | HttpHelper 예외 처리 → onPostExecute() 오류 분기 |
| 비정량10-2 | 화면 유지 | O | - | try-catch 처리 |
| 비정량11-1 | 재조회 목록 갱신 | O | - | ProgressDlgShipSearch 재호출 + deleteAll 패턴 |
| 비정량11-2 | 추가/삭제 반영 | O | - | 동일 |
| 비정량11-3 | 기존 계근 데이터 유지 | O | - | TB_GOODS_WET 독립 테이블 유지 |

---

## 5. 사이드이펙트

- 이 가이드는 **코드를 읽기만 하며 수정하지 않는다**
- 검증 중 발견된 오류 코드가 있으면 `app/doc/오류/` 에 별도 문서화 후 개발 가이드 작성

---

## 6. 데이터 저장 구조

### AI 정적 검증 핵심 조건 매핑

| 검증 항목 | 조건/변수 | 소스 위치 |
|----------|---------|---------|
| 정량 반올림 적용 | `Common.searchType.equals(SEARCH_TYPE_EMART)` | BixolonShipmentActivity:1661 |
| 반올림 수식 | `Math.round(centerWorkWeight * 100.0) / 100.0` | BixolonShipmentActivity:1662 |
| 수기 입력 반올림 | `String.format("%.1f", weight_double)` | BixolonShipmentActivity:660 |
| 비정량 중복 차단 제외 | `SEARCH_TYPE_NONFIXED \|\| SEARCH_TYPE_HOMEPLUS_NONFIXED` | BixolonShipmentActivity:1187, 1308 |
| 중복 차단 메시지 | `"이미 스캔한 바코드입니다.\n다른 바코드를 스캔하세요."` | BixolonShipmentActivity:1193, 1318 |
| 소비기한 트리거 조건1 | `PACKER_CODE=KILKOY && STORE_CODE=MEAT_CENTER_STORE` | BixolonShipmentActivity:678 |
| 소비기한 트리거 조건2 | `CENTERNAME.contains(TRD\|WET\|E/T) && (EMART\|LOTTE)` | BixolonShipmentActivity:697-698 |
| 비정량 바코드타입 제한 제거 | JSP 129행 주석 처리 | search_production_nonfixed.jsp |
| 비정량 라벨 분기 | `searchType.equals(SEARCH_TYPE_NONFIXED)` | BixolonShipmentActivity:1700 |
| M9 라벨 케이스 | `case "M9":` | LabelPrintHelper:648 |
| M8 라벨 케이스 | `case "M8":` | LabelPrintHelper:663 |
| 비정량 바코드정보 JSP URL | `URL_SEARCH_BARCODE_INFO_NONFIXED` | ProgressDlgBarcodeSearch:83 |

---

## 7. 호출 시점

```
[MainActivity]
    ├── btnEmart (이마트 출하대상받기) → ProgressDlgShipSearch (searchType="0")
    ├── btnproductionNonfixedlist (비정량 출하대상받기) → ProgressDlgShipSearch (searchType="4")
    │       ↓ 완료 후
    │   onPostExecute() → TB_SHIPMENT INSERT + ProgressDlgBarcodeSearch 자동 호출
    │       ↓ 완료 후
    │   TB_BARCODE_INFO INSERT
    ├── btnEmart2 (이마트 계근입력시작) → BixolonShipmentActivity (searchType="0")
    └── btnProdNonfixedWet (비정량 계근입력시작) → BixolonShipmentActivity (searchType="4")
            ↓ 계근 완료
        GoodsWetSendTask → insert_goods_wet.jsp (searchType 0)
                         → insert_goods_wet_nonfixed.jsp (searchType 4)
```

---

## 8. 개발 플랜

### Step 1: 정량 시나리오 1~5 정적 검증 (핵심 로직)

**Part 1. 분석**
- 메서드: `BixolonShipmentActivity` 계근 처리 메서드, `ProgressDlgShipSearch.doInBackground()`
- 범위: BixolonShipmentActivity.java 전체 / ProgressDlgShipSearch.java
- 용도: 정상 흐름, 중량 반올림, 중복 차단, 소비기한 트리거 코드 존재 및 조건 검증
- 주의할 점: searchType "0"인 경우에만 반올림 적용됨을 코드로 확인, 소비기한 트리거는 수기입력 경로에만 존재함을 확인

| # | 검증 항목 | 소스 위치 | 검증 내용 |
|---|---------|---------|---------|
| 1 | 출하대상 JSP URL | ProgressDlgShipSearch:130 | searchType 0 → URL_SEARCH_PRODUCTION 호출 |
| 2 | 바코드정보 없는 상품 안내 | ProgressDlgBarcodeSearch.onPostExecute() | 빈 결과 시 다이얼로그 분기 |
| 3 | 중량 반올림 분기 | BixolonShipmentActivity:1661-1662 | searchType EMART만 반올림 적용 |
| 4 | 수기 입력 반올림 | BixolonShipmentActivity:660 | format("%.1f") |
| 5 | 중복 스캔 차단 메서드 | DBHandler.duplicatequeryGoodsWet_check() | 정량 경로에서 호출됨 |
| 6 | 소비기한 트리거 조건 | BixolonShipmentActivity:678, 697-698 | KILKOY+MEAT_CENTER / TRD,WET,E/T 센터명 |
| 7 | 소비기한 누락 차단 | BixolonShipmentActivity:1266, 1275 | Toast 메시지 코드 |

**Part 2. 변환 계획**
- 변환 방식: 코드 읽기(Read/Grep) 전용 — 수정 없음
- 주의사항: 검증 결과를 이 문서의 Part 6에 직접 기록

**체크리스트**
- [ ] Part 1: 출하대상 JSP URL 분기 코드 확인
- [ ] Part 1: 바코드정보 없는 상품 안내 분기 확인
- [ ] Part 1: 중량 반올림 searchType 분기 확인
- [ ] Part 1: 수기 입력 반올림 코드 확인
- [ ] Part 1: 중복 스캔 차단 메서드 확인
- [ ] Part 1: 소비기한 트리거 조건 확인
- [ ] Part 1: 소비기한 누락 시 계근 차단 Toast 확인
- [ ] Part 6: 검증 결과 기록

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

### Step 2: 정량 시나리오 6~12 정적 검증 (라벨·전송·예외)

**Part 1. 분석**
- 메서드: `GoodsWetSendTask.doInBackground()`, `LabelPrintHelper.printLabel()`, 삭제 핸들러
- 범위: BixolonShipmentActivity.java (전송 파트, 삭제 파트), LabelPrintHelper.java
- 용도: 이마트 양식 라벨 분기, 전송 패킷 구조, 재출력 Handler, 빈 날짜/서버 미연결 예외, 재조회 동기화, 창고 파라미터, 삭제 로직 검증
- 주의할 점: 라벨 내용 육안 확인 항목은 정적 검증 불가로 명시

| # | 검증 항목 | 소스 위치 | 검증 내용 |
|---|---------|---------|---------|
| 1 | 이마트 라벨 분기 | LabelPrintHelper.printLabel() | BARCODE_TYPE별 case 분기 |
| 2 | MESSAGE_REPRINT Handler | BixolonShipmentActivity | REPRINT 메시지 처리 코드 |
| 3 | 전송 패킷 구조 | GoodsWetSendTask.doInBackground() | packet 문자열 구성 |
| 4 | 전송 완료 후 계근완료 갱신 | updatequeryShipment() 호출 | arSM 어댑터 갱신 |
| 5 | 서버 미연결 예외 처리 | HttpHelper.sendDataDb() try-catch | onPostExecute 오류 분기 |
| 6 | 빈 목록 처리 | ProgressDlgShipSearch.onPostExecute() | 결과 없음 처리 |
| 7 | 재조회 시 데이터 초기화 | deleteAll + insertAll 패턴 | TB_SHIPMENT 재갱신 |
| 8 | 창고 파라미터 포함 여부 | ProgressDlgShipSearch 파라미터 생성 | WH_CODE 포함 확인 |
| 9 | 삭제 다이얼로그 코드 | 삭제 버튼 핸들러 | AlertDialog.Builder |
| 10 | 삭제 후 중복 체크 통과 | duplicatequeryGoodsWet 삭제 후 결과 | 없음으로 반환 |

**Part 2. 변환 계획**
- 변환 방식: 코드 읽기(Read/Grep) 전용 — 수정 없음
- 주의사항: 라벨 육안 확인(6-3~5)은 디바이스 실측 필요로 구분

**체크리스트**
- [ ] Part 1: 이마트 라벨 분기 확인
- [ ] Part 1: MESSAGE_REPRINT 처리 확인
- [ ] Part 1: 전송 패킷 구조 확인
- [ ] Part 1: 전송 완료 후 계근완료 갱신 확인
- [ ] Part 1: 서버 미연결 예외 처리 확인
- [ ] Part 1: 빈 목록 처리 확인
- [ ] Part 1: 재조회 deleteAll+insertAll 패턴 확인
- [ ] Part 1: 창고 파라미터 포함 확인
- [ ] Part 1: 삭제 다이얼로그 + 중복 체크 통과 확인
- [ ] Part 6: 검증 결과 기록

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

### Step 3: 비정량 시나리오 1~5 정적 검증 (핵심 로직)

**Part 1. 분석**
- 메서드: `ProgressDlgShipSearch` 비정량 분기, `ProgressDlgBarcodeSearch` 비정량 분기, 중복 허용 로직
- 범위: ProgressDlgShipSearch.java, ProgressDlgBarcodeSearch.java, BixolonShipmentActivity.java
- 용도: 비정량 출하대상 URL 분기, 바코드타입 제한 제거 JSP 확인, 중복 허용 코드, 비정량 반올림 미적용 확인
- 주의할 점: 비정량2(바코드타입 제한 제거)는 JSP 소스 직접 확인 필요

| # | 검증 항목 | 소스 위치 | 검증 내용 |
|---|---------|---------|---------|
| 1 | 비정량 JSP URL | ProgressDlgShipSearch:151-153 | searchType "4" → URL_SEARCH_PRODUCTION_NONFIXED |
| 2 | 바코드타입 제한 제거 | search_production_nonfixed.jsp:128-129 | `AND BARCODE_TYPE IN ('M8','M9')` 주석 처리 |
| 3 | BARCODE_TYPE 컬럼 SELECT 포함 | search_production_nonfixed.jsp:55 | COALESCE(M1.바코드타입, M2.바코드타입) AS BARCODE_TYPE |
| 4 | 비정량 중복 허용 분기 | BixolonShipmentActivity:1187 | NONFIXED → 중복 체크 skip |
| 5 | 비정량 중복 허용 분기 (2번째) | BixolonShipmentActivity:1308 | 동일 |
| 6 | 비정량 반올림 미적용 | BixolonShipmentActivity:1661 | EMART만 round → NONFIXED 미해당 |
| 7 | 비정량 바코드정보 URL | ProgressDlgBarcodeSearch:82-83 | searchType "4" → URL_SEARCH_BARCODE_INFO_NONFIXED |

**Part 2. 변환 계획**
- 변환 방식: 코드 읽기(Read/Grep) 전용 — 수정 없음
- 주의사항: JSP는 외부 Tomcat 경로에서 확인

**체크리스트**
- [ ] Part 1: 비정량 JSP URL 분기 확인
- [ ] Part 1: search_production_nonfixed.jsp 바코드타입 조건 주석 처리 확인
- [ ] Part 1: BARCODE_TYPE SELECT 포함 확인
- [ ] Part 1: 중복 허용 분기 2개 위치 확인
- [ ] Part 1: 비정량 반올림 미적용 확인
- [ ] Part 1: 비정량 바코드정보 URL 확인
- [ ] Part 6: 검증 결과 기록

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

### Step 4: 비정량 시나리오 6~11 정적 검증 (라벨·전송·예외)

**Part 1. 분석**
- 메서드: `LabelPrintHelper` M8/M9 case, `GoodsWetSendTask` 비정량 분기, 재조회 로직
- 범위: LabelPrintHelper.java, BixolonShipmentActivity.java (전송 파트)
- 용도: M8/M9 라벨 분기 코드, 물류코드 null 처리, 비정량 전송 패킷, 복수 건 루프, 재조회 동기화 검증
- 주의할 점: 라벨 물류코드 육안 확인은 디바이스 실측 필요로 구분

| # | 검증 항목 | 소스 위치 | 검증 내용 |
|---|---------|---------|---------|
| 1 | 비정량 라벨 분기 진입 | BixolonShipmentActivity:1700-1701 | SEARCH_TYPE_NONFIXED → 비정량 라벨 출력 |
| 2 | M9 케이스 코드 | LabelPrintHelper:648 | case "M9": 분기 존재 |
| 3 | M8 케이스 코드 | LabelPrintHelper:663 | case "M8": 분기 존재 |
| 4 | 물류코드 빈값 처리 | LabelPrintHelper M8/M9 메서드 | null/empty 방어 코드 |
| 5 | 복수 건 전송 루프 | GoodsWetSendTask:2701 이하 | list_send_info 루프 |
| 6 | 비정량 전송 패킷 | GoodsWetSendTask NONFIXED 분기 | packet 구조 확인 |
| 7 | TB_GOODS_WET 독립 유지 | DBHandler.createqueryGoodsWet() | 재조회와 독립된 테이블 |
| 8 | 비정량 빈 날짜 처리 | ProgressDlgShipSearch.onPostExecute() | 빈 결과 처리 |
| 9 | 비정량 서버 미연결 예외 | HttpHelper 예외 → onPostExecute() | 오류 분기 |
| 10 | 비정량 재조회 동기화 | ProgressDlgShipSearch 재호출 | deleteAll + insertAll |

**Part 2. 변환 계획**
- 변환 방식: 코드 읽기(Read/Grep) 전용 — 수정 없음
- 주의사항: 물류코드 육안 확인(비정량6-2~3)은 디바이스 실측 필요로 구분

**체크리스트**
- [ ] Part 1: 비정량 라벨 분기 진입 확인
- [ ] Part 1: M9/M8 case 분기 존재 확인
- [ ] Part 1: 물류코드 빈값 방어 코드 확인
- [ ] Part 1: 복수 건 전송 루프 확인
- [ ] Part 1: 비정량 전송 패킷 구조 확인
- [ ] Part 1: TB_GOODS_WET 독립 유지 확인
- [ ] Part 1: 빈 날짜/서버 미연결 예외 확인
- [ ] Part 1: 재조회 동기화 deleteAll+insertAll 패턴 확인
- [ ] Part 6: 검증 결과 기록

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

### Step 5: 통합 검증 보고

| # | 검증 항목 | 확인 |
|:-:|---------|------|
| 1 | 정량 시나리오 1~12 정적 검증 항목 전체 Pass | □ |
| 2 | 비정량 시나리오 1~11 정적 검증 항목 전체 Pass | □ |
| 3 | 디바이스 실측 필요 항목 목록 정리 완료 | □ |
| 4 | 검증 중 발견된 오류 코드 → 오류 문서 생성 여부 확인 | □ |
| 5 | 정량/비정량 반올림 분기 소스 위치 최종 확인 | □ |
| 6 | 바코드타입 제한 제거 JSP 주석 처리 최종 확인 | □ |
| 7 | 소비기한 트리거 경로(수기 전용) 최종 확인 | □ |
| 8 | 비정량 중복 허용 분기 2개 위치 최종 확인 | □ |

---

### 개발 순서 요약

```
Step 1: 정량 시나리오 1~5 정적 검증 (핵심 로직: 반올림·중복차단·소비기한)
    ↓
Step 2: 정량 시나리오 6~12 정적 검증 (라벨·전송·예외·재조회·삭제)
    ↓
Step 3: 비정량 시나리오 1~5 정적 검증 (핵심 로직: 바코드타입제한제거·중복허용·반올림미적용)
    ↓
Step 4: 비정량 시나리오 6~11 정적 검증 (라벨M8/M9·물류코드·전송·예외)
    ↓
Step 5: 통합 검증 보고
```

---

## 9. 테스트 시나리오

### AI 정적 검증 가능 항목 요약 (23개 시나리오 기준)

```
정량 시나리오 1~12
    AI 정적 검증 가능: 출하대상 URL 분기 / 반올림 조건 / 중복차단 메서드 /
                       소비기한 트리거 조건 / 전송 패킷 구조 / 예외 처리 /
                       재조회 deleteAll+insertAll / 창고 파라미터 / 삭제 로직
    디바이스 실측 필요: 실제 바코드 스캔 / 라벨 출력 육안 확인 /
                       블루투스·저울 연동 / 서버 실전송

비정량 시나리오 1~11
    AI 정적 검증 가능: 비정량 URL 분기 / 바코드타입 제한 제거 JSP /
                       중복 허용 분기 / 반올림 미적용 / M8/M9 라벨 분기 /
                       물류코드 빈값 방어 / 복수 건 전송 루프
    디바이스 실측 필요: 실제 바코드 스캔 / 라벨 출력 육안 확인 /
                       물류코드 라벨 표기 / ERP 데이터 조작
```

---

## 10. 예상 문제점 및 해결 방안

| # | 문제점 | 원인 | 해결 방안 |
|---|--------|------|----------|
| 1 | 정적 검증으로 라벨 내용 확인 불가 | 물리 프린터 출력 필요 | 디바이스 실측 항목으로 분류하고 추후 디바이스 테스트에서 확인 |
| 2 | 소비기한 트리거 재현 불가 (정적) | 실제 CENTERNAME 데이터 필요 | 코드 조건(`contains("TRD")` 등)으로 논리 검증 대체 |
| 3 | JSP 수행 결과 확인 불가 | DB 실접속 필요 | JSP 소스 쿼리 코드 읽기로 의도 검증 대체 |
| 4 | 네트워크 예외 상태 재현 불가 | 실 네트워크 차단 필요 | try-catch 코드 존재 여부로 처리 의도 검증 |
| 5 | 검증 중 오류 코드 발견 시 처리 | 예상치 못한 버그 | `app/doc/오류/` 에 즉시 문서화 후 진행 |

---

## 11. 진행 현황

| Step | 작업 | 상태 |
|------|------|------|
| 1 | 정량 시나리오 1~5 정적 검증 (핵심 로직) | ⏳ 대기 |
| 2 | 정량 시나리오 6~12 정적 검증 (라벨·전송·예외) | ⏳ 대기 |
| 3 | 비정량 시나리오 1~5 정적 검증 (핵심 로직) | ⏳ 대기 |
| 4 | 비정량 시나리오 6~11 정적 검증 (라벨·전송·예외) | ⏳ 대기 |
| 5 | 통합 검증 보고 | ⏳ 대기 |

---

## 관련 문서

- 테스트 시나리오 원본: `app/doc/테스트/03_이마트_출하계근_전체테스트.html`
- 소스분석: `app/doc/소스분석/` (해당 파일 분석 문서 참조)
- 비정량 바코드타입 제한 제거 개발: `app/doc/개발/44_비정량_이마트_타입구분_J_제외_B만_사용.md`
- 비정량 M9 바코드 신규 추가: `app/doc/개발/43_비정량_이마트_M9_바코드_신규_추가.md`
- 비정량 출하계근대상 JSP MSSQL전환: `app/doc/개발/35_비정량_출하계근대상_JSP_MSSQL전환.md`
- 비정량 바코드정보조회 JSP MSSQL전환: `app/doc/개발/36_비정량_바코드정보조회_JSP_MSSQL전환.md`

---

**문서 버전**: 1.0
