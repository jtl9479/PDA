# 출하대상 목록 팝업 추가

**작성일**: 2026-04-06
**목적**: 출하대상받기 이후 로컬 DB에 저장된 출하대상 목록을 팝업으로 확인하는 기능 추가

---

## AI 제약 조건

- 기존 WHERE 조건, 로직을 임의로 제거/추가/변경하지 않는다
- 문서에 명시된 step만 진행하고, 다음 step은 지시를 기다린다
- step 완료 후 체크리스트 + 진행 현황을 반드시 업데이트한다
- 문서에 없는 개선/리팩토링을 임의로 수행하지 않는다
- 기존 기능과 100% 동일하게 동작해야 한다

---

## 1. 현재 구조

### ActionBar 메뉴 (menu/main.xml)

```xml
<item android:id="@+id/action_pinrtsettings" android:title="프린터설정" />
<item android:id="@+id/action_daysettings" android:title="날짜설정" />
```

- 날짜설정은 ActionBar 메뉴에 위치 (`showAsAction="always|withText"`)
- 출하대상 목록 확인 기능 없음

### selectqueryAllShipment() 현재 상태

```java
// DBHandler.java:373~407 - GI_D_ID 1개 컬럼만 SELECT
String sqlStr = "SELECT " + DBInfo.GI_D_ID
    + " FROM " + DBInfo.TABLE_NAME_SHIPMENT
    + " ORDER BY GI_D_ID ASC";
```

- **용도**: 데이터 존재 여부 체크 (list.size() > 0)
- **사용처**: MainActivity.startWeighing() (395줄), ProgressDlgShipSearch (101줄)
- **팝업용으로 사용 불가**: ITEM_CODE, ITEM_NAME, GI_REQ_QTY, GI_REQ_PKG를 가져오지 않음

### 문제점

- 출하대상받기 후 데이터가 정상 저장되었는지 확인할 방법이 없음
- 계근입력시작까지 진입해야 출하대상 리스트 확인 가능
- 기존 selectqueryAllShipment()는 GI_D_ID만 조회하므로 팝업 표시에 사용 불가

---

## 2. 변경 구조

### 데이터 흐름

```
[ActionBar 메뉴에 "출하대상" 추가]
    ↓
[사용자 클릭]
    ↓
[DBHandler.selectqueryShipmentForPopup() 호출] ← 신규 메서드
    ↓
[TB_SHIPMENT LEFT JOIN TB_GOODS_WET → 계근수량 포함 조회]
    ↓
[AlertDialog 팝업에 목록 표시]
    ↓
[컬럼: 품목코드 | 품목명 | searchType | 출하중량 | 출하수량 | 계근수량]
```

### 팝업 컬럼 정의

| # | 컬럼 | Shipments_Info getter | 비고 |
|:-:|------|:--------------------:|------|
| 1 | 품목코드 | getITEM_CODE() | |
| 2 | 품목명 | getITEM_NAME() | |
| 3 | searchType | Common.searchType | 전역변수 (0=이마트 등) |
| 4 | 출하중량 | getGI_REQ_QTY() | |
| 5 | 출하수량 | getGI_REQ_PKG() | |
| 6 | 계근수량 | getPACKING_QTY() | TB_GOODS_WET COUNT (LEFT JOIN) |

---

## 3. 수정 대상 파일

| # | 파일 | 위치 | 수정 내용 |
|:-:|------|------|----------|
| 1 | **menu/main.xml** | app/src/main/res/menu/ | "출하대상" 메뉴 아이템 추가 |
| 2 | **DBHandler.java** | selectqueryShipmentForPopup() | 팝업용 SELECT 신규 메서드 추가 |
| 3 | **MainActivity.java** | onOptionsItemSelected(), showShipmentListDialog() | 메뉴 분기 + 팝업 표시 메서드 추가 |

---

## 4. 수정 상세

### 4.1 menu/main.xml

**경로**: `app/src/main/res/menu/main.xml`

**변경 후 (기존 항목 유지, 신규 추가):**

```xml
<item
    android:id="@+id/action_shipmentlist"
    android:orderInCategory="100"
    android:title="출하대상"
    app:showAsAction="always|withText"/>
```

### 4.2 DBHandler.java - 신규 메서드 추가

**경로**: `app/src/main/java/.../db/DBHandler.java`
**추가 위치**: selectqueryAllShipment() (407줄) 뒤

```java
/**
 * 출하대상 팝업 표시용 SELECT (품목코드, 품목명, 출하중량, 출하수량, 계근수량)
 * TB_SHIPMENT LEFT JOIN TB_GOODS_WET으로 계근수량 포함
 */
public static ArrayList<Shipments_Info> selectqueryShipmentForPopup(Context context) {
    ArrayList<Shipments_Info> list_si = new ArrayList<Shipments_Info>();
    DBHelper mDbHelper = new DBHelper(context);
    mDbHelper.open();

    try {
        Cursor cursor;
        String sqlStr = "SELECT "
                + "S." + DBInfo.ITEM_CODE + ", "
                + "S." + DBInfo.ITEM_NAME + ", "
                + "S." + DBInfo.GI_REQ_QTY + ", "
                + "S." + DBInfo.GI_REQ_PKG + ", "
                + "COUNT(W." + DBInfo.GI_D_ID + ") AS WET_CNT"
                + " FROM "
                + DBInfo.TABLE_NAME_SHIPMENT + " S"
                + " LEFT JOIN " + DBInfo.TABLE_NAME_GOODS_WET + " W"
                + " ON S." + DBInfo.GI_D_ID + " = W." + DBInfo.GI_D_ID
                + " GROUP BY S." + DBInfo.GI_D_ID + ", S." + DBInfo.ITEM_CODE
                + ", S." + DBInfo.ITEM_NAME + ", S." + DBInfo.GI_REQ_QTY
                + ", S." + DBInfo.GI_REQ_PKG
                + " ORDER BY S." + DBInfo.ITEM_CODE + " ASC";

        cursor = mDbHelper.selectSql(sqlStr);
        if (Common.D) {
            Log.v(TAG, "selectqueryShipmentForPopup -> " + sqlStr);
            Log.v(TAG, "selectqueryShipmentForPopup -> " + cursor.getCount());
        }

        Shipments_Info si;
        while (cursor.moveToNext()) {
            si = new Shipments_Info();
            si.setITEM_CODE(Common.nullCheck(cursor.getString(cursor.getColumnIndex("ITEM_CODE")), ""));
            si.setITEM_NAME(Common.nullCheck(cursor.getString(cursor.getColumnIndex("ITEM_NAME")), ""));
            si.setGI_REQ_QTY(Common.nullCheck(cursor.getString(cursor.getColumnIndex("GI_REQ_QTY")), ""));
            si.setGI_REQ_PKG(Common.nullCheck(cursor.getString(cursor.getColumnIndex("GI_REQ_PKG")), ""));
            si.setPACKING_QTY(cursor.getInt(cursor.getColumnIndex("WET_CNT")));
            list_si.add(si);
        }
        cursor.close();
    } catch (Exception e) {
        if (Common.D) {
            Log.v(TAG, "selectqueryShipmentForPopup exception -> " + e.getMessage().toString());
        }
    }
    mDbHelper.close();
    return list_si;
}
```

**기존 selectqueryAllShipment() 변경 없음** (GI_D_ID만 SELECT, 존재 여부 체크용 그대로 유지)

### 4.3 MainActivity.java

**경로**: `app/src/main/java/.../MainActivity.java`

**onOptionsItemSelected()에 추가 (145줄 부근):**

```java
if (id == R.id.action_shipmentlist) {
    showShipmentListDialog();
    return true;
}
```

**showShipmentListDialog() 신규 메서드:**

```java
private void showShipmentListDialog() {
    ArrayList<Shipments_Info> list = DBHandler.selectqueryShipmentForPopup(this);

    if (list.size() == 0) {
        Toast.makeText(this, "출하대상이 없습니다.", Toast.LENGTH_SHORT).show();
        return;
    }

    StringBuilder sb = new StringBuilder();
    sb.append(String.format("%-10s %-15s %-5s %-8s %-8s %-8s\n",
        "품목코드", "품목명", "타입", "중량", "수량", "계근"));
    sb.append("──────────────────────────────────────\n");

    for (Shipments_Info si : list) {
        sb.append(String.format("%-10s %-15s %-5s %-8s %-8s %-8s\n",
            si.getITEM_CODE(),
            si.getITEM_NAME(),
            Common.searchType,
            si.getGI_REQ_QTY(),
            si.getGI_REQ_PKG(),
            String.valueOf(si.getPACKING_QTY())));
    }

    AlertDialog.Builder builder = new AlertDialog.Builder(this);
    builder.setTitle("출하대상 목록 (" + list.size() + "건)");
    builder.setMessage(sb.toString());
    builder.setPositiveButton("닫기", null);
    builder.show();
}
```

**검증**: 출하대상받기 후 "출하대상" 메뉴 클릭 → 팝업에 목록 표시

---

## 5. 사이드이펙트

- 기존 기능 변경 없음 (신규 메뉴/메서드 추가만)
- 기존 selectqueryAllShipment() 수정 없음 (그대로 유지)
- 신규 selectqueryShipmentForPopup()은 팝업 전용 조회
- 다른 Activity에 영향 없음

---

## 6. 개발 플랜

### Step 1: DBHandler 신규 메서드 추가

**Part 1. 분석**
- 대상: DBHandler.java
- 범위: selectqueryAllShipment() (407줄) 뒤에 신규 메서드 추가
- 용도: 팝업 표시용 SELECT (ITEM_CODE, ITEM_NAME, GI_REQ_QTY, GI_REQ_PKG)
- 주의할 점: 기존 selectqueryAllShipment() 변경 없음

**체크리스트**
- [x] Part 1: 분석 완료 확인
- [x] Part 2: 변환 수행
- [ ] Part 3: 컴파일 확인

**Part 6. 변경 내용**:
- **무엇을**: DBHandler.java에 selectqueryShipmentForPopup() 신규 메서드 추가
- **왜**: 기존 selectqueryAllShipment()는 GI_D_ID만 조회하므로 팝업 표시에 사용 불가
- **어떻게**: selectqueryAllShipment() (407줄) 뒤에 ITEM_CODE, ITEM_NAME, GI_REQ_QTY, GI_REQ_PKG 4개 컬럼 SELECT 메서드 추가

---

### Step 2: ActionBar 메뉴 추가 및 팝업 구현

**Part 1. 분석**
- 대상: menu/main.xml, MainActivity.java
- 범위: 메뉴 아이템 1개 추가, 메서드 2개 추가 (onOptionsItemSelected 분기, showShipmentListDialog)
- 주의할 점: 기존 메뉴(프린터설정, 날짜설정) 변경 없음

**체크리스트**
- [x] Part 1: 분석 완료 확인
- [x] Part 2: 변환 수행
- [ ] Part 3: 컴파일 확인
- [ ] Part 4: 출하대상받기 → 출하대상 메뉴 클릭 → 팝업 표시 확인

**Part 6. 변경 내용**:
- **무엇을**: menu/main.xml에 "출하대상" 메뉴 추가, MainActivity에 onOptionsItemSelected 분기 + showShipmentListDialog() 메서드 추가
- **왜**: 출하대상받기 후 데이터 확인 수단 없었음
- **어떻게**: ActionBar 메뉴 아이템 추가 → selectqueryShipmentForPopup() 호출 → AlertDialog 팝업에 5개 컬럼 표시

---

---

### Step 3: 계근수량 컬럼 추가 (LEFT JOIN TB_GOODS_WET)

**Part 1. 분석**
- 대상: DBHandler.java (selectqueryShipmentForPopup), MainActivity.java (showShipmentListDialog)
- 범위: SELECT 쿼리에 LEFT JOIN 추가, 팝업 컬럼 6개로 확장
- 용도: 현재 계근 진행 수량 확인
- 주의할 점: TB_GOODS_WET에 데이터 없으면 WET_CNT=0 (LEFT JOIN이므로 정상)

**체크리스트**
- [ ] Part 1: 분석 완료 확인
- [ ] Part 2: 변환 수행 (DBHandler 쿼리 수정 + MainActivity 팝업 수정)
- [ ] Part 3: 컴파일 확인
- [ ] Part 4: 출하대상받기 → 계근 → 출하대상 메뉴 → 계근수량 표시 확인

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

### 개발 순서 요약

```
Step 1: DBHandler.selectqueryShipmentForPopup() 신규 메서드 추가 ✅
    ↓
Step 2: menu/main.xml 메뉴 추가 + MainActivity 팝업 구현 ✅
    ↓
Step 3: 계근수량 컬럼 추가 (LEFT JOIN TB_GOODS_WET) ⏳
```

---

## 7. 진행 현황

| Step | 작업 | 상태 |
|------|------|------|
| 1 | DBHandler 신규 메서드 추가 | ✅ 완료 |
| 2 | ActionBar 메뉴 추가 및 팝업 구현 | ✅ 완료 |
| 3 | 계근수량 컬럼 추가 (LEFT JOIN) | ⏳ 대기 |

---

**문서 버전**: 1.2
