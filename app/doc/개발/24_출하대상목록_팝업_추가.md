# 출하대상 목록 팝업 추가

**작성일**: 2026-04-06
**목적**: 출하대상받기 이후 로컬 DB에 저장된 출하대상 목록을 팝업으로 확인하는 기능 추가

---

## AI 제약 조건

- 기존 WHERE 조건, 로직을 임의로 제거/추가/변경하지 않는다
- 문서에 명시된 step만 진행하고, 다음 step은 지시를 기다린다
- step 완료 후 체크리스트 + 진행 현황을 반드시 업데이트한다
- 문서에 없는 개선/리팩토링을 임의로 수행하지 않는다
- 기존 기능과 100% 동일하게 동작해야 한다

---

## 1. 현재 구조

### ActionBar 메뉴 (menu/main.xml)

```xml
<item android:id="@+id/action_pinrtsettings" android:title="프린터설정" />
<item android:id="@+id/action_daysettings" android:title="날짜설정" />
```

- 날짜설정은 ActionBar 메뉴에 위치 (`showAsAction="always|withText"`)
- 출하대상 목록 확인 기능 없음

### 문제점

- 출하대상받기 후 데이터가 정상 저장되었는지 확인할 방법이 없음
- 계근입력시작까지 진입해야 출하대상 리스트 확인 가능

---

## 2. 변경 구조

### 데이터 흐름

```
[ActionBar 메뉴에 "출하대상" 추가]
    ↓
[사용자 클릭]
    ↓
[DBHandler.selectqueryAllShipment() 호출]
    ↓
[AlertDialog 팝업에 ListView 표시]
    ↓
[컬럼: 품목코드 | 품목명 | searchType | 출하중량 | 출하수량]
```

### 팝업 컬럼 정의

| # | 컬럼 | Shipments_Info getter | 비고 |
|:-:|------|:--------------------:|------|
| 1 | 품목코드 | getITEM_CODE() | |
| 2 | 품목명 | getITEM_NAME() | |
| 3 | searchType | Common.searchType | 전역변수 (0=이마트 등) |
| 4 | 출하중량 | getGI_REQ_QTY() | |
| 5 | 출하수량 | getGI_REQ_PKG() | |

---

## 3. 수정 대상 파일

| # | 파일 | 위치 | 수정 내용 |
|:-:|------|------|----------|
| 1 | **menu/main.xml** | app/src/main/res/menu/ | "출하대상" 메뉴 아이템 추가 |
| 2 | **MainActivity.java** | onOptionsItemSelected() | 출하대상 메뉴 클릭 시 팝업 표시 |

---

## 4. 수정 상세

### 4.1 menu/main.xml

**경로**: `app/src/main/res/menu/main.xml`

**변경 후:**

```xml
<item
    android:id="@+id/action_shipmentlist"
    android:orderInCategory="100"
    android:title="출하대상"
    app:showAsAction="always|withText"/>
```

### 4.2 MainActivity.java

**경로**: `app/src/main/java/.../MainActivity.java`

**onOptionsItemSelected()에 추가:**

```java
if (id == R.id.action_shipmentlist) {
    showShipmentListDialog();
    return true;
}
```

**showShipmentListDialog() 신규 메서드:**

```java
private void showShipmentListDialog() {
    ArrayList<Shipments_Info> list = DBHandler.selectqueryAllShipment(this);

    if (list.size() == 0) {
        Toast.makeText(this, "출하대상이 없습니다.", Toast.LENGTH_SHORT).show();
        return;
    }

    // 헤더 + 데이터 문자열 구성
    StringBuilder sb = new StringBuilder();
    sb.append(String.format("%-10s %-15s %-5s %-8s %-8s\n",
        "품목코드", "품목명", "타입", "중량", "수량"));
    sb.append("─────────────────────────────────\n");

    for (Shipments_Info si : list) {
        sb.append(String.format("%-10s %-15s %-5s %-8s %-8s\n",
            si.getITEM_CODE(),
            si.getITEM_NAME(),
            Common.searchType,
            si.getGI_REQ_QTY(),
            si.getGI_REQ_PKG()));
    }

    AlertDialog.Builder builder = new AlertDialog.Builder(this);
    builder.setTitle("출하대상 목록 (" + list.size() + "건)");
    builder.setMessage(sb.toString());
    builder.setPositiveButton("닫기", null);
    builder.show();
}
```

**검증**: 출하대상받기 후 "출하대상" 메뉴 클릭 → 팝업에 목록 표시

---

## 5. 사이드이펙트

- 기존 기능 변경 없음 (신규 메뉴/메서드 추가만)
- DB 조회는 기존 `selectqueryAllShipment()` 재사용
- 다른 Activity에 영향 없음

---

## 6. 개발 플랜

### Step 1: ActionBar 메뉴 추가 및 팝업 구현

**Part 1. 분석**
- 대상: menu/main.xml, MainActivity.java
- 범위: 메뉴 아이템 1개 추가, 메서드 1개 추가
- 주의할 점: 기존 메뉴(프린터설정, 날짜설정) 변경 없음

**Part 2. 변환 계획**
- menu/main.xml에 action_shipmentlist 아이템 추가
- MainActivity.onOptionsItemSelected()에 분기 추가
- showShipmentListDialog() 메서드 추가

**체크리스트**
- [ ] Part 1: 분석 완료 확인
- [ ] Part 2: 변환 계획 확인
- [ ] Part 3: 변환 수행
- [ ] Part 4: 컴파일 확인
- [ ] Part 5: 출하대상받기 → 출하대상 메뉴 클릭 → 팝업 표시 확인

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

## 7. 진행 현황

| Step | 작업 | 상태 |
|------|------|------|
| 1 | ActionBar 메뉴 추가 및 팝업 구현 | ⏳ 대기 |

---

**문서 버전**: 1.0
