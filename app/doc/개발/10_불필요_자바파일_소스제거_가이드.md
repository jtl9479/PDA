# 불필요 자바 파일/소스 제거 가이드

**작성일**: 2026-02-02
**최종 검토일**: 2026-02-02
**검증 범위**: Java 30개 파일, AndroidManifest.xml

---

## 1. 개요

### 1.1 목적
- 미사용 자바 파일 삭제
- 미사용 import, 변수, 주석 코드 제거
- 코드 가독성 및 유지보수성 향상
- APK 파일 크기 감소

### 1.2 현재 파일 현황

```
app/src/main/java/com/rgbsolution/highland_emart/
├── Activity (7개)
│   ├── LoginActivity.java
│   ├── MainActivity.java
│   ├── ShipmentActivity.java
│   ├── BixolonShipmentActivity.java
│   ├── ProductionActivity.java
│   ├── ExpiryEnterActivity.java
│   └── SettingActivity.java
│
├── adapter/ (3개)
│   ├── ShipmentListAdapter.java
│   ├── DetailAdapter.java
│   └── UnknownAdapter.java
│
├── common/ (6개)
│   ├── Common.java
│   ├── HttpHelper.java
│   ├── Base64.java                    ← 삭제 대상
│   ├── ProgressDlgShipSearch.java
│   ├── ProgressDlgBarcodeSearch.java
│   ├── ProgressDlgGoodsWetSearch.java
│   └── ProgressDlgNewBarcodeInfo.java ← 삭제 대상
│
├── db/ (3개)
│   ├── DBInfo.java
│   ├── DBHelper.java
│   └── DBHandler.java
│
├── items/ (3개)
│   ├── Shipments_Info.java
│   ├── Barcodes_Info.java
│   └── Goodswets_Info.java
│
├── print/ (3개)
│   ├── BluetoothPrintService.java
│   ├── BixolonSocketPrinter.java
│   └── DeviceListActivity.java
│
├── scanner/ (3개)
│   ├── HoneywellScannerActivity.java
│   ├── ScannerActivity.java           ← 삭제 대상
│   └── Constants.java                 ← 삭제 대상
│
└── test/ (1개)
    └── TestDataHelper.java            ← 삭제 대상 (운영 배포 시)
```

---

## 2. 삭제 대상 파일

### 2.1 확정 삭제 (4개 파일)

| # | 파일명 | 경로 | 삭제 사유 | 검증 명령어 |
|:-:|--------|------|-----------|-------------|
| 1 | **Base64.java** | common/ | 프로젝트 전체에서 import/사용 없음 | `grep -r "import.*Base64" → 없음` |
| 2 | **ScannerActivity.java** | scanner/ | PM80 스캐너용 레거시, HoneywellScannerActivity로 완전 대체됨 | `grep -r "import.*ScannerActivity" → HoneywellScannerActivity만 import됨` |
| 3 | **Constants.java** | scanner/ | Bluebird PDA용 상수, 현재 Honeywell EDA51 사용으로 미사용 | `grep -r "import.*Constants" → 없음` |
| 4 | **ProgressDlgNewBarcodeInfo.java** | common/ | 호출하는 곳 없음 | `grep -r "import.*ProgressDlgNewBarcodeInfo" → 없음` |

### 2.2 운영 배포 시 삭제 (1개 파일)

| # | 파일명 | 경로 | 삭제 사유 | 비고 |
|:-:|--------|------|-----------|------|
| 5 | **TestDataHelper.java** | test/ | 테스트 전용 클래스 | MainActivity에서 사용 중, 함께 정리 필요 |

### 2.3 삭제 불가 (검증 완료)

| 파일명 | 사용 위치 | 비고 |
|--------|-----------|------|
| BluetoothPrintService.java | ShipmentActivity.java (라인 246, 487, 848, 3171) | Woosim 프린터 통신 - **실제 사용 중** |
| ProgressDlgGoodsWetSearch.java | ProgressDlgBarcodeSearch.java (라인 199) | 바코드 조회 후 호출 - **실제 사용 중** |
| UnknownAdapter.java | ProgressDlgGoodsWetSearch.java (라인 178) | 미등록 바코드 목록 표시 - **실제 사용 중** |

---

## 3. 코드 정리 대상

### 3.1 LoginActivity.java (5개 항목)

| # | 라인 | 삭제 대상 | 사유 | 검증 결과 |
|:-:|:----:|-----------|------|-----------|
| 1 | 25 | `import com.rgbsolution.highland_emart.print.BluetoothPrintService;` | import만 있고 실제 사용 안 함 | `grep "mPrintService" → 52행 선언만 있음` |
| 2 | 35 | `import com.sgis.labelengine.*;` | import만 있고 전혀 사용 안 함 | `grep "labelengine" → import만 있음` |
| 3 | 37 | `import java.security.MessageDigest;` | 29행과 중복 | 중복 import |
| 4 | 52 | `private BluetoothPrintService mPrintService = null;` | 선언만 있고 사용 안 함 | 미사용 변수 |
| 5 | 344-356 | `private final Handler mHandler = new Handler() {...}` | 내부 로직 비어있음 (break만 있음) | 미사용 Handler |

### 3.2 ExpiryEnterActivity.java (2개 항목)

| # | 라인 | 삭제 대상 | 사유 |
|:-:|:----:|-----------|------|
| 1 | 22 | `//private Button buttonClose;` | 주석 코드 |
| 2 | 28 | `//Intent intent;` | 주석 코드 |

### 3.3 MainActivity.java (운영 배포 시 - 9개 항목)

| # | 라인 | 삭제 대상 | 사유 |
|:-:|:----:|-----------|------|
| 1 | 21 | `import com.rgbsolution.highland_emart.test.TestDataHelper;` | 테스트용 import |
| 2 | 100-106 | 테스트 데이터 초기화 블록 | 테스트용 코드 |
| 3 | 219-220 | `TestDataHelper.deleteAllTestData/insertTestDataForEmart` | 테스트용 코드 |
| 4 | 237-238 | `TestDataHelper...insertTestDataForHomeplus` | 테스트용 코드 |
| 5 | 250-251 | `TestDataHelper...insertTestDataForWholesale` | 테스트용 코드 |
| 6 | 263-264 | `TestDataHelper...insertTestDataForEmartNonfixed` | 테스트용 코드 |
| 7 | 276-277 | `TestDataHelper...insertTestDataForHomeplusNonfixed` | 테스트용 코드 |
| 8 | 289-290 | `TestDataHelper...insertTestDataForLotte` | 테스트용 코드 |

### 3.4 ~~ShipmentActivity.java~~ - 정리 불필요

> **검토 결과**: 기존 문서에서 언급된 주석 코드들이 현재 소스에 존재하지 않음 (이미 정리됨)
> - `makingDateInput` 상수: 존재하지 않음
> - 주석 코드들: 검색 결과 없음
> - `REQUEST_CONNECT_DEVICE_INSECURE`: 라인 4015에서 **실제 사용 중** (삭제 불가)

### 3.5 ~~ProductionActivity.java~~ - 정리 불필요

> **검토 결과**: 기존 문서에서 언급된 주석 코드들이 현재 소스에 존재하지 않음 (이미 정리됨)
> - 86-87행 테스트 주석: 존재하지 않음
> - 191행 `//scan_flag = true;`: 존재하지 않음

---

## 4. 수정 순서

```
┌─────────────────────────────────────────────────────────────────┐
│                        수정 순서                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ═══════════════════ Phase 1. 파일 삭제 ═══════════════════     │
│                                                                 │
│  Step 1. Base64.java 삭제                                       │
│      │   경로: common/Base64.java                               │
│      │   의존성: 없음                                            │
│      ▼                                                          │
│  Step 2. Constants.java 삭제                                    │
│      │   경로: scanner/Constants.java                           │
│      │   의존성: 없음                                            │
│      ▼                                                          │
│  Step 3. ScannerActivity.java 삭제                              │
│      │   경로: scanner/ScannerActivity.java                     │
│      │   의존성: 없음 (HoneywellScannerActivity로 대체됨)         │
│      ▼                                                          │
│  Step 4. ProgressDlgNewBarcodeInfo.java 삭제                    │
│      │   경로: common/ProgressDlgNewBarcodeInfo.java            │
│      │   의존성: 없음                                            │
│      ▼                                                          │
│                                                                 │
│  ═══════════════════ Phase 2. 코드 정리 ═══════════════════     │
│                                                                 │
│  Step 5. LoginActivity.java 정리                                │
│      │   미사용 import 3개, 미사용 변수 1개, 빈 Handler 삭제      │
│      ▼                                                          │
│  Step 6. ExpiryEnterActivity.java 정리                          │
│      │   주석 코드 2개 삭제                                      │
│      ▼                                                          │
│  Step 7. 컴파일 테스트                                          │
│      ▼                                                          │
│                                                                 │
│  ═══════════════════ Phase 3. 운영 배포 시 ═════════════════    │
│                                                                 │
│  Step 8. TestDataHelper.java 삭제                               │
│      │    경로: test/TestDataHelper.java                        │
│      ▼                                                          │
│  Step 9. MainActivity.java 테스트 코드 삭제                      │
│      │    import 1개, 호출 코드 8개소 삭제                       │
│      ▼                                                          │
│  Step 10. 최종 컴파일 및 테스트                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 진행 체크리스트

### Phase 1. 파일 삭제

| Step | 파일 | 작업 | 완료 |
|:----:|------|------|:----:|
| 1 | Base64.java | 파일 삭제 | ⬜ |
| 2 | Constants.java | 파일 삭제 | ⬜ |
| 3 | ScannerActivity.java | 파일 삭제 | ⬜ |
| 4 | ProgressDlgNewBarcodeInfo.java | 파일 삭제 | ⬜ |

### Phase 2. 코드 정리

| Step | 파일 | 작업 | 완료 |
|:----:|------|------|:----:|
| 5 | LoginActivity.java | 미사용 import 3개, 변수 1개, Handler 1개 삭제 | ⬜ |
| 6 | ExpiryEnterActivity.java | 주석 코드 2개 삭제 | ⬜ |
| 7 | - | 컴파일 테스트 | ⬜ |

### Phase 3. 운영 배포 시

| Step | 파일 | 작업 | 완료 |
|:----:|------|------|:----:|
| 8 | TestDataHelper.java | 파일 삭제 | ⬜ |
| 9 | MainActivity.java | 테스트 코드 삭제 (import + 호출 8개소) | ⬜ |
| 10 | - | 최종 컴파일 및 테스트 | ⬜ |

---

## 6. Step별 수정 상세

### Step 1. Base64.java 삭제

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/common/Base64.java`

**검증 결과**:
```bash
grep -r "Base64" --include="*.java" app/src/
# 결과: 자기 자신(Base64.java)만 검색됨
```

**조치**: 파일 전체 삭제

---

### Step 2. Constants.java 삭제

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/scanner/Constants.java`

**검증 결과**:
```bash
grep -r "Constants\." --include="*.java" app/src/
# 결과: 없음 (미사용)
```

**조치**: 파일 전체 삭제

---

### Step 3. ScannerActivity.java 삭제

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/scanner/ScannerActivity.java`

**검증 결과**:
```bash
grep -r "extends ScannerActivity" --include="*.java" app/src/
# 결과: 없음

grep -r "ScannerActivity" --include="*.java" app/src/
# 결과: 주석(@see ScannerActivity)에서만 언급

# AndroidManifest.xml에도 등록되어 있지 않음
```

**대체 클래스**: `HoneywellScannerActivity.java`
- ShipmentActivity → extends HoneywellScannerActivity
- BixolonShipmentActivity → extends HoneywellScannerActivity
- ProductionActivity → extends HoneywellScannerActivity

**조치**: 파일 전체 삭제

---

### Step 4. ProgressDlgNewBarcodeInfo.java 삭제

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/common/ProgressDlgNewBarcodeInfo.java`

**검증 결과**:
```bash
grep -r "ProgressDlgNewBarcodeInfo" --include="*.java" app/src/
# 결과: 자기 자신만 검색됨 (호출하는 곳 없음)
```

**조치**: 파일 전체 삭제

---

### Step 5. LoginActivity.java 정리

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/LoginActivity.java`

**삭제할 import (3개)**:

| 라인 | 삭제 코드 |
|:----:|-----------|
| 25 | `import com.rgbsolution.highland_emart.print.BluetoothPrintService;` |
| 35 | `import com.sgis.labelengine.*;` |
| 37 | `import java.security.MessageDigest;` (중복, 29행에 이미 있음) |

**삭제할 변수 (1개)**:

| 라인 | 삭제 코드 |
|:----:|-----------|
| 52 | `private BluetoothPrintService mPrintService = null;` |

**삭제할 Handler (1개)**:

| 라인 | 삭제 코드 |
|:----:|-----------|
| 340-352 | `private final Handler mHandler = new Handler() { ... }` |

---

### Step 6. ExpiryEnterActivity.java 정리

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/ExpiryEnterActivity.java`

**삭제할 주석 코드 (2개)**:

| 라인 | 삭제 코드 |
|:----:|-----------|
| 22 | `//private Button buttonClose;` |
| 28 | `//Intent intent;` |

---

### Step 7. 컴파일 테스트

```bash
# Android Studio에서 컴파일 테스트
Build → Rebuild Project
```

**확인 사항**:
- 컴파일 에러 없음
- APK 생성 성공

---

### Step 8. TestDataHelper.java 삭제 (운영 배포 시)

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/test/TestDataHelper.java`

**검증 결과**:
```bash
grep -r "TestDataHelper" --include="*.java" app/src/
# 결과: MainActivity.java에서 사용 중 (테스트용)
```

**조치**:
1. 먼저 Step 9에서 MainActivity.java의 호출 코드 삭제
2. 그 후 TestDataHelper.java 파일 삭제
3. test/ 디렉토리 삭제 (빈 폴더가 됨)

---

### Step 9. MainActivity.java 테스트 코드 삭제 (운영 배포 시)

**파일**: `app/src/main/java/com/rgbsolution/highland_emart/MainActivity.java`

**삭제할 import (1개)**:

| 라인 | 삭제 코드 |
|:----:|-----------|
| 21 | `import com.rgbsolution.highland_emart.test.TestDataHelper;` |

**삭제할 테스트 코드 블록 (9개소)**:

| 라인 | 삭제 코드 | 위치 |
|:----:|-----------|------|
| 100-106 | 테스트 데이터 초기화 블록 | onCreate() |
| 219-220 | `TestDataHelper.deleteAllTestData/insertTestDataForEmart` | 이마트 버튼 |
| 237-238 | `TestDataHelper...insertTestDataForHomeplus` | 홈플러스 버튼 |
| 250-251 | `TestDataHelper...insertTestDataForWholesale` | 도매 버튼 |
| 263-264 | `TestDataHelper...insertTestDataForEmartNonfixed` | 비정량 버튼 |
| 276-277 | `TestDataHelper...insertTestDataForHomeplusNonfixed` | HP비정량 버튼 |
| 289-290 | `TestDataHelper...insertTestDataForLotte` | 롯데 버튼 |

---

## 7. 예상 결과

### 7.1 파일 변화

| 구분 | 삭제 전 | 삭제 후 | 감소 |
|------|:-------:|:-------:|:----:|
| Java 파일 수 | 30개 | 26개 | -4개 |
| Phase 3 후 | 26개 | 25개 | -1개 |

### 7.2 삭제되는 파일 목록

**Phase 1-2 (즉시)**:
```
app/src/main/java/com/rgbsolution/highland_emart/
├── common/
│   ├── Base64.java                    ← 삭제
│   └── ProgressDlgNewBarcodeInfo.java ← 삭제
└── scanner/
    ├── ScannerActivity.java           ← 삭제
    └── Constants.java                 ← 삭제
```

**Phase 3 (운영 배포 시)**:
```
app/src/main/java/com/rgbsolution/highland_emart/
└── test/
    └── TestDataHelper.java            ← 삭제
```

### 7.3 예상 효과

- 불필요 코드 제거로 유지보수성 향상
- 빌드 시간 단축
- APK 파일 크기 감소 (예상 ~30KB)
- 코드 가독성 향상
- 레거시 코드 정리 (PM80 → Honeywell EDA51 전환 완료)

---

## 8. 주의사항

### 8.1 삭제 전 확인

```bash
# 삭제 전 반드시 grep으로 최종 확인
grep -r "클래스명" --include="*.java" app/src/
```

### 8.2 백업

- Git에서 이전 상태로 복원 가능
- 삭제 전 커밋하여 롤백 지점 생성

### 8.3 컴파일 테스트

- 각 Step 완료 후 컴파일 테스트 필수
- Android Studio → Build → Rebuild Project

---

## 9. 핵심 요약

```
Phase 1. 파일 삭제 (4개)
├── Base64.java (미사용)
├── Constants.java (Bluebird PDA용, 현재 Honeywell 사용)
├── ScannerActivity.java (PM80 레거시, HoneywellScannerActivity로 대체)
└── ProgressDlgNewBarcodeInfo.java (미사용)

Phase 2. 코드 정리 (2개 파일)
├── LoginActivity.java (import 3개, 변수 1개, Handler 1개)
└── ExpiryEnterActivity.java (주석 2개)

Phase 3. 운영 배포 시 (2개 파일)
├── TestDataHelper.java (파일 삭제)
└── MainActivity.java (테스트 코드 8개소 삭제)

※ ShipmentActivity.java, ProductionActivity.java: 이미 정리 완료됨 (정리 불필요)
```

---

**문서 작성일**: 2026-02-02
**관련 문서**: 클래스별_불필요소스_정리.md, 소스코드_정리_개요.md
