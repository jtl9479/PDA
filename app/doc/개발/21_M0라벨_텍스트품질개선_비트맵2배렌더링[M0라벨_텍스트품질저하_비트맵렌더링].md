# M0 라벨 텍스트 품질 개선 — 비트맵 2배 렌더링

**작성일**: 2026-04-03
**목적**: slcsBitmapText의 비트맵 렌더링 품질 개선 (2배 크기 렌더링 후 축소)
**관련 오류**: `app/doc/오류/07_M0라벨_텍스트품질저하_비트맵렌더링[이마트출하대상받기].md`

---

## AI 제약 조건

- 기존 WHERE 조건, 로직을 임의로 제거/추가/변경하지 않는다
- 문서에 명시된 step만 진행하고, 다음 step은 지시를 기다린다
- step 완료 후 체크리스트 + 진행 현황을 반드시 업데이트한다
- 문서에 없는 개선/리팩토링을 임의로 수행하지 않는다
- 기존 기능과 100% 동일하게 동작해야 한다

---

## 1. 현재 구조

### LabelPrintHelper.java:251~291 — slcsBitmapText()

```java
private byte[] slcsBitmapText(int x, int y, int size, String text, boolean bold) {
    Paint paint = new Paint();
    paint.setAntiAlias(false);
    paint.setTextSize(size);              // 원본 크기로 렌더링

    int textWidth = (int) Math.ceil(paint.measureText(text));
    int textHeight = (int) Math.ceil(fm.descent - fm.ascent);

    Bitmap bitmap = Bitmap.createBitmap(textWidth, textHeight, Bitmap.Config.ARGB_8888);
    Canvas canvas = new Canvas(bitmap);
    canvas.drawText(text, 0, -fm.ascent, paint);

    // 1bpp 흑백 변환
    for (int row = 0; row < textHeight; row++) {
        for (int col = 0; col < textWidth; col++) {
            int pixel = bitmap.getPixel(col, row);
            int gray = (Color.red(pixel) + Color.green(pixel) + Color.blue(pixel)) / 3;
            if (gray < 128) { bitmapData[byteIndex] |= (1 << bitIndex); }
        }
    }
}
```

### 문제점

- 1:1 해상도로 렌더링 후 1bpp 변환 → 글씨 가장자리 계단현상(jagging)
- 안티앨리어싱 OFF 상태에서 작은 크기의 텍스트는 품질 저하 심함

---

## 2. 변경 구조

### 개선 방식

```
[현재]
size로 렌더링 → 1bpp 변환 → 프린터 전송

[변경]
size*2로 렌더링 → 축소(createScaledBitmap) → 1bpp 변환 → 프린터 전송
```

- 2배 크기로 렌더링하면 글씨 가장자리 정보가 더 풍부
- 축소 시 안티앨리어싱 효과로 계단현상 감소
- 1bpp 변환 시 더 정확한 흑백 판정

---

## 3. 수정 대상 파일

| # | 파일 | 위치 | 수정 내용 |
|:-:|------|------|----------|
| 1 | **LabelPrintHelper.java** | 251~291행 | slcsBitmapText 메서드 — 2배 렌더링 후 축소 |

---

## 4. 수정 상세

### 4.1 LabelPrintHelper.java — slcsBitmapText()

**경로**: `app/src/main/java/.../print/LabelPrintHelper.java`

**변경 전 (256~274행):**

```java
paint.setAntiAlias(false);
paint.setTextSize(size);

int textWidth = (int) Math.ceil(paint.measureText(text));
Paint.FontMetrics fm = paint.getFontMetrics();
int textHeight = (int) Math.ceil(fm.descent - fm.ascent);
if (textWidth <= 0 || textHeight <= 0) return new byte[0];

Bitmap bitmap = Bitmap.createBitmap(textWidth, textHeight, Bitmap.Config.ARGB_8888);
Canvas canvas = new Canvas(bitmap);
canvas.drawColor(Color.WHITE);
canvas.drawText(text, 0, -fm.ascent, paint);
```

**변경 후:**

```java
// 2배 크기로 렌더링 (품질 개선)
int scale = 2;
paint.setAntiAlias(true);           // 2배 렌더링 시 안티앨리어싱 ON
paint.setTextSize(size * scale);

int bigWidth = (int) Math.ceil(paint.measureText(text));
Paint.FontMetrics fm = paint.getFontMetrics();
int bigHeight = (int) Math.ceil(fm.descent - fm.ascent);
if (bigWidth <= 0 || bigHeight <= 0) return new byte[0];

// 2배 크기 비트맵에 렌더링
Bitmap bigBitmap = Bitmap.createBitmap(bigWidth, bigHeight, Bitmap.Config.ARGB_8888);
Canvas canvas = new Canvas(bigBitmap);
canvas.drawColor(Color.WHITE);
canvas.drawText(text, 0, -fm.ascent, paint);

// 원본 크기로 축소 (안티앨리어싱 적용)
int textWidth = bigWidth / scale;
int textHeight = bigHeight / scale;
Bitmap bitmap = Bitmap.createScaledBitmap(bigBitmap, textWidth, textHeight, true);
bigBitmap.recycle();
```

**검증**: M0 라벨 인쇄 후 원본 라벨과 텍스트 선명도 비교

---

## 5. 사이드이펙트

### 모든 한글 텍스트에 영향

- `slcsBitmapText()`는 모든 라벨 타입(M0, M1, E0~E3 등)에서 공통 사용
- 품질 개선이므로 부정적 사이드이펙트 없음
- 렌더링 시간 미세 증가 가능 (2배 비트맵 생성 → 축소)

### 라벨 크기/위치 영향

- `textWidth`, `textHeight`는 축소 후 원본과 동일
- LD 명령의 좌표(x, y)는 변경 없음
- 라벨 레이아웃에 영향 없음

---

## 6. 데이터 저장 구조

해당 없음 (렌더링 방식 변경만, 데이터 구조 변경 없음)

---

## 7. 호출 시점

해당 없음 (기존 호출 흐름 변경 없음, 렌더링 품질만 변경)

---

## 8. 개발 플랜

### Step 1: slcsBitmapText 2배 렌더링 수정

**Part 1. 분석**
- 메서드: slcsBitmapText()
- 범위: LabelPrintHelper.java:251~291
- 용도: 비트맵 2배 렌더링 후 축소로 텍스트 품질 개선
- 주의할 점: textWidth/textHeight는 축소 후 원본과 동일해야 함

| # | 항목 | 위치 | 내용 |
|---|------|------|------|
| 1 | scale 변수 추가 | 256행 | `int scale = 2;` |
| 2 | AntiAlias ON | 256행 | `paint.setAntiAlias(true);` |
| 3 | textSize 2배 | 257행 | `paint.setTextSize(size * scale);` |
| 4 | bigBitmap 생성 | 270행 | 2배 크기 Bitmap 생성 |
| 5 | 축소 | 274행 | `createScaledBitmap(bigBitmap, textWidth, textHeight, true)` |
| 6 | bigBitmap.recycle() | 275행 | 메모리 해제 |

**Part 2. 변환 계획**
- 변환 방식: 기존 메서드 내부만 수정
- 주의사항: 메서드 시그니처(파라미터, 반환값) 변경 없음

**체크리스트**
- [ ] Part 1: 분석 완료 확인
- [ ] Part 2: 변환 계획 확인
- [ ] Part 3: 변환 수행
- [ ] Part 4: 컴파일 확인
- [ ] Part 5: 단위테스트
- [ ] Part 6: 회귀테스트

**Part 6. 변경 내용** (완료 후 작성):
- **무엇을**:
- **왜**:
- **어떻게**:

---

### Step 2: 라벨 인쇄 테스트

| # | 테스트 | 확인 |
|:-:|--------|------|
| 1 | M0 라벨 인쇄 → 텍스트 선명도 원본과 비교 | □ |
| 2 | 라벨 레이아웃(좌표, 크기) 변경 없음 확인 | □ |
| 3 | 바코드 정상 출력 확인 | □ |
| 4 | 한글/영문/숫자 모두 정상 출력 | □ |
| 5 | 앱 비정상 종료 없음 | □ |

---

### 개발 순서 요약

```
Step 1: slcsBitmapText 2배 렌더링 수정
    ↓
Step 2: 라벨 인쇄 테스트
```

---

## 9. 테스트 시나리오

### 시나리오 1: M0 라벨 품질 비교

```
1. 수정 전 M0 라벨 (20260403M0.jpg) 보관
2. 코드 수정 후 동일 데이터로 M0 라벨 인쇄
3. 원본 라벨(M0원본.jpg)과 비교
4. 텍스트 선명도, 레이아웃 확인
```

### 시나리오 2: 품질 부족 시

```
방법 3 (프린터 TTF 다운로드)으로 전환
→ Bixolon_SPP-L3000_폰트적용_가이드.md 참고
```

---

## 10. 예상 문제점 및 해결 방안

| # | 문제점 | 원인 | 해결 방안 |
|---|--------|------|----------|
| 1 | 렌더링 시간 증가 | 2배 비트맵 생성 + 축소 | 체감 차이 미미 (ms 단위) |
| 2 | 메모리 사용 증가 | 2배 비트맵 임시 생성 | bigBitmap.recycle()로 즉시 해제 |
| 3 | 품질 개선 부족 | 비트맵 방식 한계 | 방법 3 (프린터 TTF 다운로드)로 전환 |

---

## 11. 진행 현황

| Step | 작업 | 상태 |
|------|------|------|
| 1 | slcsBitmapText 2배 렌더링 수정 | ⏳ 대기 |
| 2 | 라벨 인쇄 테스트 | ⏳ 대기 |

---

**문서 버전**: 1.0
