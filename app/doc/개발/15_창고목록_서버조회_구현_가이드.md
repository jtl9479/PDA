# 창고 목록 서버 조회 구현 가이드

**작성일**: 2026-03-18
**목적**: LoginActivity의 하드코딩 창고 목록을 서버 조회로 대체

---

## 1. 현재 구조 (하드코딩)

### LoginActivity.java

```java
// line 57 — 하드코딩 배열
static final String[] store = {"부산2","이천1센터","삼일냉장","SWC","탑로지스"};

// line 90~94 — Spinner에 직접 바인딩
ArrayAdapter adapter = new ArrayAdapter(this, android.R.layout.simple_spinner_item, store);
Spinner spin = (Spinner) findViewById(R.id.storeSpinner);
spin.setAdapter(adapter);

// line 99~102 — 선택 시 창고명만 저장
Common.selectWarehouse = storetv.getText().toString();  // 예: "삼일냉장"
```

### ProgressDlgShipSearch.java

```java
// line 117~127 — 창고명 → 코드 하드코딩 매핑 (4회 반복, 총 20개 if-else)
if (Common.selectWarehouse.equals("삼일냉장")) {
    data += " AND GR_WAREHOUSE_CODE = 'IN10273'";
} else if (Common.selectWarehouse.equals("SWC")) {
    data += " AND GR_WAREHOUSE_CODE = 'IN60464'";
} else if (Common.selectWarehouse.equals("이천1센터")) {
    data += " AND GR_WAREHOUSE_CODE = '4001'";
} else if (Common.selectWarehouse.equals("부산센터")) {
    data += " AND GR_WAREHOUSE_CODE = '4004'";
} else if (Common.selectWarehouse.equals("탑로지스")) {
    data += " AND GR_WAREHOUSE_CODE = 'IN63279'";
}
// 위 패턴이 searchType 0, 2, 3, 6에서 4번 반복
```

### 문제점

1. 창고 추가/변경 시 앱 소스 수정 및 재배포 필요
2. 동일 매핑 코드 4회 반복 (유지보수 어려움)
3. 창고명 오타 존재 (롯데 searchType=6에서 "이천센터"로 "1" 누락, 이마트/홈플러스/도매는 "이천1센터")
4. 롯데(searchType=6)는 dbid="highland"로 다른 DB를 사용 (이마트/홈플러스/도매는 "inno")

---

## 2. 변경 구조 (서버 조회)

### 데이터 흐름

```
[앱 시작]
    ↓
[LoginActivity.onCreate()]
    ↓ search_warehouse.jsp 호출
[서버] CO_창고관리 테이블 조회
    ↓ 응답: "20::IN10273::삼일냉장;;20::IN60464::SWC;;..."
[LoginActivity] 파싱 → Common에 저장 (selectCompanyCode, warehouseCodes, warehouseNames)
    ↓
[Spinner] 창고명 목록 표시
    ↓ 사용자 선택
[Common] selectCompanyCode = "20", selectWarehouse = "삼일냉장", selectWarehouseCode = "IN10273"
    ↓
[ProgressDlgShipSearch] data += " AND 창고코드 = '" + Common.selectWarehouseCode + "'"
```

---

## 3. JSP (이미 작성됨)

### search_warehouse.jsp

```
파일 위치: app/doc/JSP/search_warehouse.jsp
URL: Common.URL_SEARCH_WAREHOUSE
```

```sql
SELECT 회사코드 AS COMPANY_CODE, 창고코드 AS WAREHOUSE_CODE, 창고명 AS WAREHOUSE_NAME
FROM CO_창고관리
WHERE 회사코드 = '20'
```

### 응답 형식

```
20::IN10273::삼일냉장;;20::IN60464::SWC;;20::4001::이천1센터;;...
```

| Index | 컬럼 | 설명 |
|:-----:|------|------|
| 0 | COMPANY_CODE | 회사코드 |
| 1 | WAREHOUSE_CODE | 창고코드 |
| 2 | WAREHOUSE_NAME | 창고명 |

> **참고**: JSP out.println에 COMPANY_CODE 출력 추가 필요 (현재 WAREHOUSE_CODE, WAREHOUSE_NAME만 출력 중)

---

## 4. 수정 대상 파일

| # | 파일 | 수정 내용 |
|:-:|------|----------|
| 1 | **Common.java** | URL 추가, 창고 목록/코드 변수 추가 |
| 2 | **LoginActivity.java** | 서버 조회 AsyncTask 추가, Spinner 동적 구성 |
| 3 | **ProgressDlgShipSearch.java** | 하드코딩 if-else 제거, Common.selectWarehouseCode 사용 |

---

## 5. 수정 상세

### 5.1 Common.java

**추가 항목:**

```java
// URL 추가
public static final String URL_SEARCH_WAREHOUSE = BASE_URL + "/search_warehouse.jsp";

// 창고 목록 (서버 조회 결과 저장)
public static ArrayList<String> warehouseNames = new ArrayList<>();   // 창고명 목록 (Spinner 표시)
public static ArrayList<String> warehouseCodes = new ArrayList<>();   // 창고코드 목록 (쿼리 조건)

// 선택된 창고코드 (신규)
public static String selectWarehouseCode = "";                         // 창고코드 (쿼리 WHERE 조건)
```

**변경 항목:**

```java
// 변경 전
public static String COMPANY_CODE = "20";              // 하드코딩

// 변경 후
public static String selectCompanyCode = "";           // 창고 조회 시 서버에서 받아옴
```

> `COMPANY_CODE`와 별도로 ShipmentActivity/LabelPrintHelper의 `COMPANY_CODE = "610933"`은
> 라벨 인쇄용 업체코드이므로 변경하지 않음.

**기존 유지:**

```java
public static String selectWarehouse = "";    // 창고명 (기존 호환)
```

### 5.2 LoginActivity.java

**변경 전:**

```java
static final String[] store = {"부산2","이천1센터","삼일냉장","SWC","탑로지스"};

// onCreate에서 직접 Spinner 구성
ArrayAdapter adapter = new ArrayAdapter(this, android.R.layout.simple_spinner_item, store);
spin.setAdapter(adapter);
```

**변경 후:**

```java
// 하드코딩 배열 제거
// onCreate에서 서버 조회 AsyncTask 실행

@Override
protected void onCreate(Bundle savedInstanceState) {
    // ... 기존 코드 ...

    // 창고 목록 서버 조회
    new WarehouseSearchTask().execute();
}

// 창고 목록 조회 AsyncTask
class WarehouseSearchTask extends AsyncTask<Void, Void, String> {
    @Override
    protected String doInBackground(Void... params) {
        try {
            return HttpHelper.getInstance().sendDataDb("", "inno",
                "search_warehouse", Common.URL_SEARCH_WAREHOUSE);
        } catch (Exception e) {
            Log.e(TAG, "창고 목록 조회 실패: " + e.getMessage());
            return "";
        }
    }

    @Override
    protected void onPostExecute(String receiveData) {
        receiveData = receiveData.replace("\r\n", "").replace("\n", "");

        Common.warehouseNames.clear();
        Common.warehouseCodes.clear();

        if (receiveData != null && !receiveData.isEmpty()) {
            String[] rows = receiveData.split(";;");
            for (String row : rows) {
                if (row.isEmpty()) continue;
                String[] cols = row.split("::");
                if (cols.length >= 3) {
                    // 회사코드는 첫 번째 행에서 한 번만 저장
                    if (Common.selectCompanyCode.isEmpty()) {
                        Common.selectCompanyCode = cols[0].trim();  // 회사코드
                    }
                    Common.warehouseCodes.add(cols[1].trim());  // 창고코드
                    Common.warehouseNames.add(cols[2].trim());  // 창고명
                }
            }
        }

        // 조회 실패 시 기본값
        if (Common.warehouseNames.isEmpty()) {
            Common.selectCompanyCode = "20";
            Common.warehouseNames.add("삼일냉장");
            Common.warehouseCodes.add("1");
        }

        // Spinner 구성
        ArrayAdapter<String> adapter = new ArrayAdapter<>(
            LoginActivity.this,
            android.R.layout.simple_spinner_item,
            Common.warehouseNames
        );
        adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item);

        Spinner spin = (Spinner) findViewById(R.id.storeSpinner);
        spin.setAdapter(adapter);

        spin.setOnItemSelectedListener(new AdapterView.OnItemSelectedListener() {
            public void onItemSelected(AdapterView parent, View v, int position, long id) {
                Common.selectWarehouse = Common.warehouseNames.get(position);
                Common.selectWarehouseCode = Common.warehouseCodes.get(position);
                Log.i(TAG, "창고 선택: " + Common.selectWarehouse
                    + " (" + Common.selectWarehouseCode + ")");
            }
            public void onNothingSelected(AdapterView parent) {}
        });
    }
}
```

### 5.3 ProgressDlgShipSearch.java

**변경 전 (4회 반복되는 하드코딩):**

```java
if (Common.selectWarehouse.equals("삼일냉장")) {
    data += " AND GR_WAREHOUSE_CODE = 'IN10273'";
} else if (Common.selectWarehouse.equals("SWC")) {
    data += " AND GR_WAREHOUSE_CODE = 'IN60464'";
} else if (Common.selectWarehouse.equals("이천1센터")) {
    data += " AND GR_WAREHOUSE_CODE = '4001'";
} else if (Common.selectWarehouse.equals("부산센터")) {
    data += " AND GR_WAREHOUSE_CODE = '4004'";
} else if (Common.selectWarehouse.equals("탑로지스")) {
    data += " AND GR_WAREHOUSE_CODE = 'IN63279'";
}
```

**변경 후 (1줄):**

```java
if (!Common.selectWarehouseCode.isEmpty()) {
    data += " AND 창고코드 = '" + Common.selectWarehouseCode + "'";
}
```

> 참고: 기존 `GR_WAREHOUSE_CODE`는 Oracle VIEW 컬럼명이며, 새 MSSQL 쿼리에서는 `창고코드`로 변경됨.
> 이마트(0), 홈플러스(2), 도매(3), 롯데(6) 4곳 모두 동일하게 변경.
>
> **주의**: 롯데(searchType=6)는 기존에 dbid="highland"(다른 DB)를 사용함.
> 새 MSSQL 전환 시 롯데도 동일 DB를 사용하는지 확인 필요.
> 창고명 오타("이천센터" → "이천1센터")도 코드 대체로 자동 해결됨.

---

## 6. 데이터 저장 구조

### Common 변수 매핑

| 변수 | 타입 | 용도 | 예시 |
|------|------|------|------|
| `selectCompanyCode` | String | 회사코드 (서버 조회) | "20" |
| `warehouseNames` | ArrayList&lt;String&gt; | Spinner 표시 목록 | ["삼일냉장", "SWC", "이천1센터", ...] |
| `warehouseCodes` | ArrayList&lt;String&gt; | 코드 목록 (같은 인덱스) | ["IN10273", "IN60464", "4001", ...] |
| `selectWarehouse` | String | 선택된 창고명 | "삼일냉장" |
| `selectWarehouseCode` | String | 선택된 창고코드 | "IN10273" |

### 인덱스 매핑

```
warehouseNames[0] = "삼일냉장"    ↔    warehouseCodes[0] = "IN10273"
warehouseNames[1] = "SWC"        ↔    warehouseCodes[1] = "IN60464"
warehouseNames[2] = "이천1센터"    ↔    warehouseCodes[2] = "4001"
```

Spinner에서 position으로 선택하면 같은 인덱스의 코드를 가져옴.

---

## 7. 호출 시점

```
[앱 시작]
    ↓
[LoginActivity.onCreate()]
    ├── SQLite 테이블 생성
    ├── 프린터 설정 로드
    ├── ★ 창고 목록 서버 조회 (WarehouseSearchTask)
    │       ↓ 비동기
    │   [서버 응답] → Common.warehouseNames/Codes 저장
    │       ↓
    │   [Spinner 구성] → 사용자 선택 대기
    └── ID/PW 입력 대기
            ↓
    [로그인 버튼] → 서버 인증
            ↓
    [MainActivity 이동] → Common.selectWarehouse/Code 사용
```

---

## 8. 사이드이펙트

### LoginActivity 로그인 시 회사코드 사용

```java
// LoginActivity.java:208
receiveData = HttpHelper.getInstance().loginData(Common.REG_ID, encPassword, Common.selectCompanyCode, Common.URL_LOGIN);
```

`Common.COMPANY_CODE` → `Common.selectCompanyCode`로 변경됨.
창고 조회(WarehouseSearchTask)가 비동기이므로, **조회 완료 전 로그인 시 빈값 전송 가능**.

**대응**: 조회 실패 시 기본값 `"20"` 설정으로 처리 (Step 2 코드에 포함됨).
단, 네트워크 지연으로 조회 진행 중에 로그인하면 여전히 빈값 가능.
필요 시 로그인 버튼을 조회 완료 후 활성화하는 방법 검토.

---

## 9. 테스트 시나리오

| # | 시나리오 | 예상 결과 |
|:-:|---------|----------|
| 1 | 정상 조회 | Spinner에 서버 창고 목록 표시 |
| 2 | 서버 연결 실패 | 기본값("삼일냉장") 표시 |
| 3 | 창고 선택 후 출하대상받기 | 선택한 창고코드로 WHERE 조건 구성 |
| 4 | 서버에 창고 추가 후 앱 재시작 | 새 창고가 Spinner에 자동 반영 |

---

## 10. 개발 플랜

### Step 1: Common.java 변수 추가

| 작업 | 내용 |
|------|------|
| URL 추가 | `URL_SEARCH_WAREHOUSE` |
| 변수 변경 | `COMPANY_CODE = "20"` → `selectCompanyCode = ""` |
| 변수 추가 | `warehouseNames`, `warehouseCodes`, `selectWarehouseCode` |

```java
public static final String URL_SEARCH_WAREHOUSE = BASE_URL + "/search_warehouse.jsp";
public static String selectCompanyCode = "";
public static ArrayList<String> warehouseNames = new ArrayList<>();
public static ArrayList<String> warehouseCodes = new ArrayList<>();
public static String selectWarehouseCode = "";
```

**검증**: 컴파일 확인

---

### Step 2: LoginActivity.java 서버 조회 구현

| 작업 | 내용 |
|------|------|
| 하드코딩 제거 | `static final String[] store` 삭제 |
| AsyncTask 추가 | `WarehouseSearchTask` 내부 클래스 |
| Spinner 변경 | 서버 응답 데이터로 동적 구성 |
| 선택 이벤트 | `selectWarehouse` + `selectWarehouseCode` 동시 저장 |

**검증**: 앱 실행 → 로그인 화면에서 Spinner에 서버 창고 목록 표시 확인

---

### Step 3: search_warehouse.jsp 수정 및 서버 배포

| 작업 | 내용 |
|------|------|
| JSP 수정 | out.println에 COMPANY_CODE 출력 추가 |
| JSP 배포 | `search_warehouse.jsp`를 서버에 배포 |
| DB 확인 | CO_창고관리 테이블에 데이터 존재 확인 |

JSP out.println 변경:
```java
// 변경 전
rs.getString("WAREHOUSE_CODE") + "::" + rs.getString("WAREHOUSE_NAME") + ";;"

// 변경 후
rs.getString("COMPANY_CODE") + "::" + rs.getString("WAREHOUSE_CODE") + "::" + rs.getString("WAREHOUSE_NAME") + ";;"
```

**검증**: 브라우저에서 JSP 직접 호출하여 `20::IN10273::삼일냉장;;...` 형식 응답 확인

---

### Step 4: ProgressDlgShipSearch.java 하드코딩 제거

| 작업 | 내용 |
|------|------|
| if-else 제거 | searchType 0, 2, 3, 6의 하드코딩 매핑 20개 제거 |
| 코드 대체 | `Common.selectWarehouseCode` 직접 사용 |

변경 대상 4곳:

| searchType | 라인 | 변경 내용 |
|:----------:|:----:|----------|
| 0 (이마트) | 117~127 | if-else → 1줄 |
| 2 (홈플러스) | 142~152 | if-else → 1줄 |
| 3 (도매) | 159~169 | if-else → 1줄 |
| 6 (롯데) | 187~197 | if-else → 1줄 |

```java
// 변경 후 (4곳 모두 동일)
if (!Common.selectWarehouseCode.isEmpty()) {
    data += " AND 창고코드 = '" + Common.selectWarehouseCode + "'";
}
```

**검증**: 출하대상받기 → 로그에서 쿼리 조건에 창고코드 포함 확인

---

### Step 5: 통합 테스트

| # | 테스트 | 확인 |
|:-:|--------|------|
| 1 | 앱 시작 → Spinner에 서버 창고 목록 표시 | □ |
| 2 | 창고 선택 → Common.selectWarehouseCode 로그 확인 | □ |
| 3 | 출하대상받기 → WHERE 조건에 창고코드 정상 포함 | □ |
| 4 | 서버 미연결 시 → 기본값 표시, 앱 비정상 종료 없음 | □ |
| 5 | 서버에 창고 추가 후 앱 재시작 → Spinner 반영 | □ |

---

### 개발 순서 요약

```
Step 1: Common.java 변수 추가
    ↓
Step 2: LoginActivity.java 서버 조회 + Spinner 동적 구성
    ↓
Step 3: search_warehouse.jsp 서버 배포 (개발자 직접)
    ↓
Step 4: ProgressDlgShipSearch.java 하드코딩 제거
    ↓
Step 5: 통합 테스트
```

예상 소요: Step 1~2~4는 앱 소스 수정, Step 3은 서버 배포

---

**문서 버전**: 1.1
