# 2026-09-16 개발 일정

---

## 오늘 해야 할 작업

### 1. PDA BixolonShipmentActivity 소스 분산 (개발66)

3,739줄 God Activity를 `shipment/type/` 의 **searchType별 파일 6개**로 나눈다.
먼저 타입별로 나눈 뒤, 6개 파일을 diff해 완전히 동일한 블록만 공통 파일로 뽑는다.
**생산(searchType 1, 7) 코드는 그대로 유지**하며 파일을 만들지 않는다.

| 순서 | 항목 | 확인 사항 | 상태 |
|:----:|------|----------|:----:|
| 1 | 개발 가이드 작성 | `개발/66_BixolonShipmentActivity_searchType별_클래스_분리.md` | ✅ |
| 2 | Step 0: 호출처 없는 코드 제거 | 6건 294줄 삭제, 3,739 → 3,445줄 | ✅ |
| 3 | Step 1: 인터페이스 + Factory 골격 | 신규 9개, Activity 추가 16줄(수정·삭제 0) | ✅ |
| 4 | Step 2: 도매(3) 바코드 스캔 이관 | `setBarcodeMsg` 본문 → `WholesaleType`, 접는 조건 4곳 | ⏳ |
| 5 | Step 3~7: 롯데·홈플러스·비정량·이마트 이관 | 6 → 2 → 5 → 4 → 0 순 | ⏳ |
| 6 | Step 8~11: 계근저장·상품매칭·전송·수기입력 | 축별 이관 | ⏳ |
| 7 | Step 12~14: 공통 추출 · Activity 정리 · 통합 테스트 | 6개 파일 diff 후 공통화 | ⏳ |

### 2. Step 0·1 실기기 회귀테스트

바꾸지 않은 기존 기능이 그대로인지 확인한다.

| 순서 | 항목 | 확인 사항 | 상태 |
|:----:|------|----------|:----:|
| 1 | 계근 화면 진입 | `onCreate` 변경 후 정상 진입 | ⏳ |
| 2 | 바코드 스캔 → 계근 → 라벨 출력 | 기본 흐름 유지 | ⏳ |
| 3 | **상세 팝업 합계 라벨 출력** | Step 0에서 `slcs*` 일부 삭제 — 남긴 5개 사용처 | ⏳ |
| 4 | 생산(1) 계근 | 생산 코드 미변경 확인 | ⏳ |
| 5 | 전송 | 전송 경로 유지 | ⏳ |

---

## 어제 완료된 작업

직전 일정 문서가 `2026-08-05.md` 이므로 해당 없음.

---

## 오늘 완료된 작업

| # | 작업 | 커밋 | 비고 |
|:-:|------|:----:|------|
| 1 | 개발66 가이드 작성 (Step 0~14 설계) | `ff575f3` | 개발64·65 대체, 사실오류 12건 정정 |
| 2 | Step 0: 호출처 없는 코드 294줄 제거 | `dc3e4d3` | 빌드 통과 · 원본 비교 "동작 동일" 판정 |
| 3 | Step 1: `shipment/type` 골격 9개 생성 | `a261ab3` | Activity 추가 16줄, 수정·삭제 0줄 |
| 4 | 개발66 Step 1 완료 반영 | `5014680` | 체크리스트·Part 6·진행현황 |

### Step 0 — 삭제 내역 (호출처 0건)

| 대상 | 삭제 전 라인 | 줄수 |
|---|---|---:|
| `ProgressDlgShipSelectBL` (+Javadoc) | 2685~2871 | 187 |
| `slcsBarcode`·`slcsLine`·`slcsBox` (+Javadoc) | 2396~2440 | 45 |
| `show_wetNextDialog` | 3641~3673 | 33 |
| `find_BL` | 2094~2105 | 12 |
| `scanFlag_swap` | 2044~2052 | 9 |
| 미사용 import 8개 | 51~55, 57~59 | 8 |

검증 — diff 대조(삭제 294 / 추가 1=빈 줄), 잔여 참조 0건, 인코딩·BOM 정상, 빌드 통과, `original-comparator` 에이전트 "동작 동일" 판정.

### Step 1 — 신규 파일 9개

`ShipmentType`(인터페이스) · `ShipmentTypeFactory` · `ShipmentConst` ·
`EmartType`(0) · `HomeplusType`(2) · `WholesaleType`(3) · `EmartNonfixedType`(4) · `HomeplusNonfixedType`(5) · `LotteType`(6)

Activity는 import 2줄 + 필드 1개 + `onCreate` 생성 블록만 추가했다. 이 Step에서는 생성만 하고 호출하지 않으므로 동작 변화가 없다.
구현체 메서드는 전부 `UnsupportedOperationException` 이라 Step 2 이후 배선 실수가 즉시 드러난다.

---

## 이슈/메모

- **라인 번호 주의** — Step 0으로 294줄이 빠져 개발66 본문의 라인 번호가 현재 파일과 어긋난다. Step 2 이후에는 개발66 §8 Step 0 "Part 6" 의 **오프셋 표**로 환산해 확인한다. (`setBarcodeMsg` 1143→1135, `wet_data_insert` 1884→1876, `ProgressDlgShipmentSend` 2899→2638)
- **원본 대조 기준** — 백업본 `BixolonShipmentActivity_Back.java` 는 IDE가 미사용 import 8개를 함께 정리해 원본과 라인이 다르다. 대조는 `git show HEAD:...` 를 쓴다. 본문(빈 줄 제외 3,142줄)은 완전 일치 확인.
- **Step 10 선행 조건** — 개발63(롯데 박스순번 파이프라인 복구)과 전송 구간(2899~3170)이 겹친다. 개발63 완료 후 Step 10 진행.
- **Step 4·5 선행 조건** — 홈플러스 운영 DB 데이터 0건 가능. 테스트 데이터 확보 후 착수.
- **Step 7 일정 조율** — 이마트(0)는 현재 출하계근 테스트 진행 중이라 마지막에 배치했다.
- 직전 일정 문서가 `2026-08-05.md` 로, 그 사이 기간은 일정 문서가 작성되지 않았다.

---

## 관련 문서

- `app/doc/개발/66_BixolonShipmentActivity_searchType별_클래스_분리.md`
- `app/doc/개발/64_BixolonShipmentActivity_마트사별_클래스_분리.md` — 66으로 대체됨
- `app/doc/개발/65_setBarcodeMsg_마트사별_분리.md` — 66으로 대체됨
- `app/doc/개발/63_롯데_박스순번_파이프라인_복구[36].md` — Step 10 선행
- `app/doc/개발/60_setBarcodeMsg_생산1_전용메서드_분리.md` — 생산 코드 유지 근거
- `app/doc/참고자료/오류패턴_분석.md`

---

### 상태 아이콘
- ⏳ 대기
- 🔄 진행중
- ✅ 완료
- ❌ 실패/보류
