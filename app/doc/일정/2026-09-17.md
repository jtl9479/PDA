# 2026-09-17 개발 일정

---

## 오늘 해야 할 작업

### 1. PDA BixolonShipmentActivity 소스 분산 (개발66)

| 순서 | 항목 | 확인 사항 | 상태 |
|:----:|------|----------|:----:|
| 1 | Step 7: 이마트(0) 바코드 스캔 이관 + 과도기 분기 제거 | `setBarcodeMsg` 본문 → `EmartType`, 접는 조건 5곳 | ✅ |
| 2 | Step 8: 계근 저장 + 라벨 이관 (6종) | 축별 이관 | ✅ |
| 3 | Step 9~11: 상품 매칭·전송·수기입력 (6종) | 축별 이관 | ✅ |
| 4 | Step 12: 공통 추출 (diff) | 6개 파일 diff 후 공통화 | ✅ |
| 5 | Step 13: Activity 정리 | | ✅ |
| 6 | Step 14: 통합 테스트 | | ⏳ |

### 2. Step 7 실기기 회귀테스트

| 순서 | 항목 | 확인 사항 | 상태 |
|:----:|------|----------|:----:|
| 1 | 이마트 정량 스캔 → 계근 → 라벨 | 기본 흐름 유지 | ⏳ |
| 2 | 트레이더스 센터 소비기한 검증 | `EMART`("0") 조건 유지 | ⏳ |
| 3 | 도매·롯데·홈플러스·비정량 2종 무영향 | 다른 타입 파일 diff 0 | ⏳ |
| 4 | 생산(1) 계근 | 생산 코드 미변경 확인 | ⏳ |

---

## 어제 완료된 작업

| # | 작업 | 커밋 |
|:-:|------|:----:|
| 1 | Step 5: 홈플러스 비정량(5) 바코드 스캔 이관 | `1379651` |
| 2 | 개발66 Step 5 완료 반영 및 일정 갱신 | `a894d85` |
| 3 | Step 6: 이마트 비정량(4) 바코드 스캔 이관 | `b2d78bb` |
| 4 | 문서: 개발66 Step 6 완료 반영 및 일정 갱신 | `88d0f8b` |

---

## 오늘 완료된 작업

| # | 작업 | 커밋 | 비고 |
|:-:|------|:----:|------|
| 1 | 개발66 Step 7 — 바코드 스캔 이관 이마트(0) EmartType + 과도기 분기 제거 | `35b3ff6` | Activity setBarcodeMsg 본문 411줄 삭제 → 위임 1줄, EmartType 476줄, 접은 조건 5곳(1213·1337 블록삭제 / 1302 조건만제거 / 1451·1513 if본문유지), 빌드 통과, 코드검증·원본비교 에이전트 통과 |
| 2 | 개발66 Step 8 — 계근 저장 + 라벨 이관 (6종) | `034a943` | wet_data_insert·MESSAGE_REPRINT·롯데 박스순번 초기화를 타입 파일 6개(onWeightConfirmed/reprintLabel/onShipmentLoaded)로 이관, 타입 파일 +789줄, 위임 게이트는 shipmentType != null 로 두어 생산(1)·생산라벨(7)은 기존 본문 유지, 추가 로직 0줄, 빌드 통과, 코드검증·원본비교 에이전트 모두 통과 |
| 3 | 개발66 Step 9 — 상품 매칭 이관 (6종) | `8e9c47c` | find_PackerProduct·find_PackerProductBarcodeGoods·find_work_info·find_work_info_barcodeGoods 를 타입 파일 6개로 이관(findPackerProduct + private 2종), 전부 매칭은 EmartNonfixedType(4)만 조건문 제거하고 위치 유지, 구간추출 차이(길이 체크 유무) 보존, Activity 원본 메서드는 생산(1)·ProgressDlgShipSelect 용으로 잔존, 추가 로직 0줄, 빌드 통과, 코드검증·원본비교 에이전트 모두 통과 |
| 4 | Step 8·9 코드 주석 원본 라인번호 정정 | `8e9c47c` | Step 8·9 주석에 적었던 `원본 N` 값이 현재 파일 기준이라 §7 규칙대로 Step 0 이전 원본 기준으로 정정(주석 전용, 로직 영향 없음) |
| 5 | 개발66 Step 10 — 전송 이관 (6종) | `e461e0b` | `ProgressDlgShipmentSend.doInBackground` 전송 본문을 타입 파일 6개 `send()` 로 이관. 건별(0·2·6, `insert_goods_wet.jsp`)과 일괄(3·4·5, `insert_goods_wet_new.jsp`)로 분리, Activity는 목록 조회 후 `shipmentType != null` 이면 위임하고 기존 본문은 생산(1)·생산라벨(7)용으로 보존. 패킷 필드·구분자·반환 지점 원본 동일, 추가 로직 0줄, 빌드 통과, 코드검증·원본비교 에이전트 모두 통과 |
| 6 | Step 10 publishProgress 미이관 판정 | `e461e0b` | `publishProgress` 는 `AsyncTask` 바깥인 `send()` 로 옮길 수 없어 이관하지 않았고, `onProgressUpdate` 가 no-op 이라 동작 차이 없음(양쪽 검증에서 허용 판정)으로 확인 |
| 7 | Step 10 주석 원본 라인번호 정정 | `e461e0b` | 추정값을 확인값으로 정정: 2966~2974 / 2986 / 3064~3065 / 3067~3069 / 3090 / 3057·3059 |
| 8 | 개발66 Step 11 — 수기 입력 이관 (6종) | `8509b8f` | inputBtnListener 의 work_flag==0 수기 입력 분기를 타입 파일 6개 onManualInput() 으로 이관. ShipmentType 에 onManualInput() 을 이 Step 에서 추가(총 7개 메서드). 중량 절사는 이마트(0)만 1자리, 바깥 else if 의 `\|\| LOTTE` 는 롯데만 참이라 진입 조건이 항상 성립(바깥 else 삭제), 안쪽 소비기한 창은 0·6만 유지. CENTERNAME 판정과 킬코이·미트센터 분기는 데이터 의존이라 6종 전부 보존. 추가 로직 0줄, 빌드 통과, 코드검증·원본비교 에이전트 모두 통과 |
| 9 | 개발66 Step 12 — 공통 추출 전수 diff 및 이동 0건 판정 | `c89f9f8` | 타입 파일 6개 전수 diff. 메서드 단위로 6종 완전 동일은 findPackerProduct(30줄)·find_work_info_barcodeGoods(42줄) 2개뿐인데 각각 callee 분기·상태 변경 8곳으로 기준 #3 위반. 연속 라인 단위 6종 공통은 17블록 556줄이나 순수 계산은 패킷 앞 13필드뿐이고, 마트사별 필드 추가 시 갈릴 위험이 커 이동하지 않기로 사용자 확인. 결과적으로 `common/` 미생성, 코드 변경 0건 |
| 10 | 개발66 Step 13 — Activity 정리 | `cafdb7d` | Activity 정리 3곳: (1) 클래스 Javadoc 의 searchType 표 정정(2·7 누락 보강, 4·5·6 실제와 불일치 수정, 개발66 위임 구조 단락 추가), (2) 타입 파일이 쓰지 않는 `select_position` 을 public → private 복귀(나머지 26개 공개 필드는 6종 전부 사용 중이라 유지), (3) `edit_barcode` 주석의 "Step 13에서 재검토" 표시를 재검토 결과로 교체. 로직 변경 0건, 빌드 통과, 코드검증 에이전트 통과. 생산 경로(§5.1) 호출 메서드 6개와 `shipmentType != null` 게이트 5곳 모두 유지 확인 |
| 11 | 개발67 개발 가이드 작성 — 생산(1)·생산라벨(7) 클래스 분리 계획 | `05cc6f5` | 개발66에서 제외했던 생산 2종을 ShipmentType 구현체로 이관하는 계획, Step 10개. Activity 잔여 본문 744줄(setBarcodeMsgProduction 260 / wet_data_insert 148 / find_* 계열 147 / MESSAGE_REPRINT 5 / 전송 일괄분기 104 / 수기 80) 대상. 컷오버(Factory 등록 + onCreate 조건 제거)는 Step 8에 일괄 배치. 생산라벨(7) 바코드 스캔 본문은 개발66 Step 7에서 삭제되어 Step 0 이전 커밋(dc3e4d3^)에서 복원 예정. find_work_info 는 ProgressDlgShipSelect 공유로 Activity 잔존 + 타입 파일 사본. 문서 정합성 검증 조건부 통과 → 지적 3건 반영 완료(합계 산술 정정 888→744, 개발66 "생산은 파일 만들지 않는다" 방침 번복 명시, ShipmentType/Factory stale Javadoc 정정을 Step 1 산출물에 추가) |
| 12 | 개발67 Step 1 — 골격 준비 | `2d1c199` | 개발67 Step 1 골격 준비 — ProductionType(1)·ProductionLabelType(7) 스텁 2개 신규 생성(각 ShipmentType 7개 메서드), ShipmentTypeFactory 에 case "1"·"7" 추가, ShipmentType·Factory 의 stale Javadoc 정정. **onCreate 를 건드리지 않아 실행 경로 변화 0건**(searchType 1·7 은 여전히 shipmentType==null 로 기존 Activity 본문을 탄다). Activity 파일 미변경. 빌드 통과, 코드검증 에이전트 통과(지적된 Factory Javadoc 서술 1건은 즉시 정정) |
| 13 | 개발67 Step 2 — 바코드 스캔 이관 생산(1) ProductionType | `00a332d` | setBarcodeMsgProduction 258줄을 ProductionType.onBarcodeScanned 로 이관. 접은 조건 0곳(개발60이 이미 정리), 치환 5종만. 원본 대조 차이 0줄(211↔211), Activity 미변경으로 런타임 영향 0, 빌드 통과, 코드검증 통과 |
| 14 | 개발66·67 로그 문자열 정정 | `9272798` | a. 접두어가 Log 문자열 리터럴 안까지 들어간 것을 20곳(개발66 6파일 12곳 + ProductionType 8곳) 원복. 동작 영향 없으나 로그가 실기기 테스트 판정 기준이라 원본과 일치시킴. 이후 대조 스크립트는 문자열 리터럴을 따로 비교하도록 변경 |
| 15 | 개발67 Step 3 — 바코드 스캔 이관 생산라벨(7) ProductionLabelType | `66d7a36` | 현재 Activity 에 없는 원본 공용 setBarcodeMsg 본문을 dc3e4d3^(Step 0 직전 커밋)에서 복원해 ProductionLabelType.onBarcodeScanned 로 이관. 7은 searchType 게이트 5곳(1205·1294·1329·1443·1505)이 전부 거짓이라 접는 결과가 도매(3)와 동일해 검증된 WholesaleType 본문을 기준으로 삼고 주석만 교체. 원본 대조 결과 접은 조건 5곳 외 차이 0줄, 문자열 리터럴 오염 0건. **생산(1)과 달리 ITEM_TYPE W/HW·B 블록과 킬코이·미트센터·센터명 판정이 전부 살아 있음을 확인**(개발60이 생산 전용 메서드에서 걷어낸 블록들). Activity 미변경으로 런타임 영향 0, 빌드 통과, 코드검증 에이전트 통과 |
| 16 | 개발67 Step 4 — 계근 저장 + 라벨 + 조회후처리 이관 (2종) | `ddcb519` | wet_data_insert 잔여 본문·MESSAGE_REPRINT else 경로·조회 후처리를 생산 2종의 onWeightConfirmed/reprintLabel/onShipmentLoaded 로 이관. 두 타입 모두 INSERT 일반·반올림 3자리로 동일하고 라벨만 다름(생산(1)은 원본 라벨 분기에 케이스가 없어 블록 전체 삭제, 생산라벨(7)은 setPrinting_prod 유지. 재출력은 1=setPrinting else 경로, 7=setPrinting_prod). 원본 대조 추가 0줄, 문자열 리터럴 오염 0건, 두 타입 diff는 라벨 블록 1곳뿐. Activity 미변경으로 런타임 영향 0, 개발66 6종 무영향, 빌드 통과, 코드검증 에이전트 통과 |

---

## 이슈/메모

- **생산라벨(7) 위임 대상 없음 → 종결(해당 없음)** — Step 7에서 과도기 분기를 제거하면서 7이 위임 경로를 타 NPE 가능하다는 지적이 있었으나, **7은 진입 경로가 없어 해당하지 않는다.**
  - ① 화면: `activity_main.xml` 406 — 생산라벨 버튼 2개(`btnproductionlist4print`·`btnProdWet4print`)를 감싼 `TableRow` 가 `android:visibility="gone"`
  - ② 진입점: `Common.searchType` 에 `"7"` 을 넣는 곳은 위 두 버튼 핸들러(`MainActivity` 301·347)뿐
  - ③ 상태: `Common.searchType` 은 `static = "0"` 으로 실행마다 초기화되어 과거 세션 값이 남지 않음
  - 단서: 위 `TableRow` 를 다시 노출시키면 NPE 가 살아난다. 생산라벨을 되살릴 경우 `setBarcodeMsg` 위임 앞에 7 처리를 먼저 추가해야 한다(개발66 Step 7 절 참조)
  - Step 8·9 위임은 `shipmentType != null` 게이트라 7이 살아나도 기존 본문을 탄다. 문제 지점은 `setBarcodeMsg` 한 곳뿐
- **주의 사항**: Step 10 전송은 실패 시 되돌릴 수 없다. Part 5 단위테스트는 **소량 데이터로 먼저 확인** 필요
- **Step 12 판정**: 6개 파일 전수 diff 결과 기준 3개(§5)를 모두 통과하는 공통 블록이 없어 `common/` 을 생성하지 않기로 확정. 코드 변경 0건, 문서만 커밋
- **Step 13 미조치 메모**: 클래스 Javadoc 의 "Woosim 프린터" 표기가 Bixolon 전환과 맞지 않으나 Step 13 범위(searchType 표) 밖이라 미조치 — 별건 처리 대기
- **개발66 상태**: Step 0~13 완료 + 전체 검증(코드검증·원본비교·문서정합성) 통과. Step 14(통합 테스트)는 개발67 완료 후 함께 수행 예정
- **재발 방지**: 기계 치환(Activity 필드 → `a.` 접두어) 시 Log 문자열 리터럴 내부까지 치환되지 않도록 보호 필요. 이후 원본 대조 스크립트는 문자열 리터럴을 별도로 비교한다
- **대조 방식 개선**: Step 3부터 문자열 리터럴을 마스킹해 코드와 따로 비교하고, 블록이 접히면 순서가 밀리므로 집합 비교로 판정
- 다음 작업: 개발67 Step 5 (상품 매칭 이관 2종 + find_work_info 잔류 방식 확정)

---

## 관련 문서

- `app/doc/개발/66_BixolonShipmentActivity_searchType별_클래스_분리.md`
- `app/doc/개발/67_BixolonShipmentActivity_생산1_7_클래스_분리.md`
- `app/doc/일정/2026-09-16.md`

---

### 상태 아이콘
- ⏳ 대기
- 🔄 진행중
- ✅ 완료
- ❌ 실패/보류
