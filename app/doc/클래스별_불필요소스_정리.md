# PDA-INNO 클래스별 불필요 소스 정리

각 클래스 파일 내의 불필요한 코드(미사용 import, 변수, 메서드, 중복 코드 등)를 정리한 문서입니다.

---

## 1. LoginActivity.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/LoginActivity.java`

### 1.1 중복 import
| 라인 | 코드 | 조치 |
|-----|------|------|
| 29, 37 | `import java.security.MessageDigest;` | 중복 import 제거 (37행 삭제) |

### 1.2 미사용 import
| 라인  | 코드                                 | 사유                               | 조치  |
| --- | ---------------------------------- | -------------------------------- | --- |
| 35  | `import com.sgis.labelengine.*;`   | LabelEngine 사용하지 않음              | 삭제  |
| 25  | `import ...BluetoothPrintService;` | mPrintService 변수 선언만 있고 실제 사용 안함 | 삭제  |

### 1.3 미사용 변수
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 52 | `private BluetoothPrintService mPrintService = null;` | 선언만 있고 사용되지 않음 | 삭제 |

### 1.4 미사용 Handler
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 340-352 | `private final Handler mHandler = new Handler() {...}` | 주석에 "기존 호환용"이라 되어있으나 내부 로직 비어있음 | 삭제 검토 |

### 1.5 정리 후 코드
```java
// 삭제 대상
import java.security.MessageDigest;  // 37행 (중복)
import com.sgis.labelengine.*;        // 35행 (미사용)
import com.rgbsolution.highland_emart.print.BluetoothPrintService; // 25행 (미사용)

private BluetoothPrintService mPrintService = null;  // 52행 (미사용)
private final Handler mHandler = new Handler() {...} // 340-352행 (미사용)
```

---

## 2. MainActivity.java ✅ 정리 완료

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/MainActivity.java`

### 2.1 미사용/불필요 변수 ✅ 삭제 완료 (2026-01-06)
| 라인     | 코드                                 | 사유                                    | 조치      |
| ------ | ---------------------------------- | ------------------------------------- | ------- |
| ~~56~~ | `private String chkProdShip = "";` | 일부 case에서 설정되지만 실제 검증에는 searchType 사용 | ✅ 삭제 완료 |
| -      | `chkProdShip = "ship";` (234행)     | 변수 삭제에 따른 할당문                         | ✅ 삭제 완료 |
| -      | `chkProdShip = "prod";` (280행)     | 변수 삭제에 따른 할당문                         | ✅ 삭제 완료 |
| -      | `chkProdShip = "homplus";` (320행)  | 변수 삭제에 따른 할당문                         | ✅ 삭제 완료 |
| -      | `chkProdShip = "lotte";` (439행)    | 변수 삭제에 따른 할당문                         | ✅ 삭제 완료 |
| -      | `chkProdShip = "prod";` (702행)     | 변수 삭제에 따른 할당문                         | ✅ 삭제 완료 |

### 2.2 중복 코드 (리팩토링 대상) - 미처리

**날짜 포맷팅 로직 8회 이상 반복** (예: 231-260행, 286-303행 등)

```java
// 동일 로직이 onClick() 내에서 8번 이상 반복됨
if(Common.selectDay == ""){
    calendar.set(Calendar.YEAR, calendar.get(Calendar.YEAR));
    // ... 동일 로직 반복
}
```

### 2.3 권장 리팩토링 - 미처리

```java
// 유틸 메서드로 추출 권장
private String getCurrentDateFormatted() {
    if(Common.selectDay.isEmpty()) {
        Calendar cal = Calendar.getInstance();
        return String.format("%04d%02d%02d",
            cal.get(Calendar.YEAR),
            cal.get(Calendar.MONTH) + 1,
            cal.get(Calendar.DAY_OF_MONTH));
    }
    return Common.selectDay;
}
```

### 2.4 주석 처리된 미사용 코드 ✅ 삭제 완료 (2026-01-06)
| 라인 | 내용 | 조치 |
|-----|------|------|
| ~~204-219~~ | 날짜 변경 AlertDialog 주석 코드 | ✅ 삭제 완료 |
| ~~359~~ | `//chkProdShip = "homplus";` 주석 | ✅ 삭제 완료 |
| ~~398~~ | `//chkProdShip = "homplus";` 주석 | ✅ 삭제 완료 |
| ~~478-483~~ | chkProdShip 검증 주석 코드 | ✅ 삭제 완료 |
| ~~509-514~~ | chkProdShip 검증 주석 코드 | ✅ 삭제 완료 |
| ~~544-549~~ | chkProdShip 검증 주석 코드 | ✅ 삭제 완료 |
| ~~592~~ | `//chkProdShip = "homplus";` 주석 | ✅ 삭제 완료 |
| ~~860~~ | `//log.Write("프로그램 종료");` 주석 | ✅ 삭제 완료 |

---

## 3. ShipmentActivity.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/ShipmentActivity.java`

### 3.1 불필요 import (검토 필요)
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 47-49 | `import java.io.ByteArrayOutputStream;` | 실제 사용 여부 확인 필요 | 검토 |

### 3.2 미사용 상수
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 190 | `public static final String makingDateInput = "";` | 빈 문자열로 선언, 사용처 없음 | 삭제 |
| 193 | `private static final int REQUEST_CONNECT_DEVICE_INSECURE = 2;` | 사용처 확인 필요 | 검토 |

### 3.3 주석 처리된 코드
| 라인 | 내용 | 조치 |
|-----|------|------|
| 386 | `//sp_center_name.setSelection(0);` | 삭제 |
| 393 | `//sp_center_name.setOnItemClickListener(...)` | 삭제 |
| 451 | `//new ProgressDlgShipSelect(...).execute();` | 삭제 |

### 3.4 파일 크기 문제
- **현재 크기**: 약 256KB (매우 큼)
- **권장**: Fragment 또는 ViewModel로 분할

---

## 4. ProductionActivity.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/ProductionActivity.java`

### 4.1 주석 처리된 코드
| 라인 | 내용 | 조치 |
|-----|------|------|
| 86-87 | `/*edit_pp_code.setText("22872"); ...*/` | 테스트 코드 삭제 |
| 191 | `//scan_flag = true;` | 삭제 |

### 4.2 미사용 변수 (검토)
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 252 | `boolean successFlag = false;` | 로컬 사용, 문제 없음 | 유지 |

---

## 5. ExpiryEnterActivity.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/ExpiryEnterActivity.java`

### 5.1 주석 처리된 코드
| 라인 | 내용 | 조치 |
|-----|------|------|
| 22 | `//private Button buttonClose;` | 삭제 |
| 28 | `//Intent intent;` | 삭제 |

### 5.2 미사용 변수 (검토)
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 41 | `final Double weightReceivedDbl;` | intent에서 받아서 다시 보내므로 사용됨 | 유지 |

---

## 6. SettingActivity.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/SettingActivity.java`

### 6.1 미사용 import
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 3 | `import android.app.Activity;` | MODE_PRIVATE에서 사용 | 유지 |

### 6.2 상태
- 불필요 소스 없음 (정상)

---

## 7. ScannerActivity.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/scanner/ScannerActivity.java`

### 7.1 주석 처리된 코드
| 라인 | 내용 | 조치 |
|-----|------|------|
| 89-99 | `/*if (mScanner.aDecodeGetTriggerMode() ...*/` | 삭제 |
| 107-108 | `//getSupportActionBar()...` | 삭제 |

### 7.2 상태
- 대체로 정상, 주석 코드만 정리

---

## 8. Common.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/common/Common.java`

### 8.1 주석 처리된 코드
| 라인 | 내용 | 조치 |
|-----|------|------|
| 12 | `// public static final String BASE_URL = "...운영서버";` | 유지 (참조용) |

### 8.2 상태
- 불필요 소스 없음 (정상)

---

## 9. Base64.java (전체 삭제 대상)

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/common/Base64.java`

### 9.1 분석
- **전체 클래스 미사용**
- 프로젝트 내 어디에서도 import 되지 않음
- Java 내장 `java.util.Base64` 또는 `android.util.Base64` 사용 권장

### 9.2 조치
- **전체 파일 삭제**

---

## 10. BluetoothPrintService.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/print/BluetoothPrintService.java`

### 10.1 분석
- ShipmentActivity에서 실제 사용됨 (464행)
- Woosim 프린터와 함께 사용

### 10.2 상태
- **삭제 불가** - 실제 사용 중

---

## 11. ProgressDlgGoodsWetSearch.java (삭제 검토)

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/common/ProgressDlgGoodsWetSearch.java`

### 11.1 분석
- 생성자: `ProgressDlgGoodsWetSearch(Context context, ArrayList<String[]> list_unknown)`
- **호출하는 곳 없음** - grep 검색 결과 호출 코드 미발견

### 11.2 조치
- **삭제 가능** (향후 기능 계획 시 유지)

---

## 12. ProgressDlgNewBarcodeInfo.java (삭제 검토)

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/common/ProgressDlgNewBarcodeInfo.java`

### 12.1 미사용 import
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 13 | `import java.util.HashMap;` | 77행에서 선언만 있고 사용 안됨 | 삭제 |

### 12.2 미사용 변수
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 78 | `HashMap<String, String> temp = new HashMap<>()` | 선언만 있고 사용 안됨 | 삭제 |

### 12.3 전체 클래스 상태
- **호출하는 곳 없음** - 삭제 가능

---

## 13. HttpHelper.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/common/HttpHelper.java`

### 13.1 Deprecated API 사용
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 3-10 | `org.apache.http.*` | Apache HttpClient deprecated (API 22+) | Retrofit/OkHttp로 교체 권장 |

### 13.2 권장 리팩토링
```java
// 현재 (deprecated)
import org.apache.http.client.HttpClient;
import org.apache.http.impl.client.DefaultHttpClient;

// 권장 (최신)
import okhttp3.OkHttpClient;
// 또는
import retrofit2.Retrofit;
```

---

## 14. DBHandler.java

**경로**: `app/src/main/java/com/rgbsolution/highland_emart/db/DBHandler.java`

### 14.1 미사용 import (검토)
| 라인 | 코드 | 사유 | 조치 |
|-----|------|------|------|
| 16 | `import java.util.HashMap;` | 사용 여부 확인 필요 | 검토 |

### 14.2 상태
- 대체로 정상

---

## 정리 요약

### 즉시 삭제 가능
| 파일 | 삭제 대상 |
|-----|----------|
| Base64.java | 전체 파일 |
| ProgressDlgGoodsWetSearch.java | 전체 파일 (검토) |
| ProgressDlgNewBarcodeInfo.java | 전체 파일 (검토) |

### 코드 정리 필요
| 파일 | 정리 항목 수 |
|-----|------------|
| LoginActivity.java | 5개 |
| MainActivity.java | 5개 + 중복코드 리팩토링 |
| ShipmentActivity.java | 3개 + 파일 분할 검토 |
| ProductionActivity.java | 2개 |
| ExpiryEnterActivity.java | 2개 |
| ScannerActivity.java | 2개 |

### 리팩토링 권장
| 파일 | 권장 사항 |
|-----|----------|
| HttpHelper.java | Apache HttpClient → OkHttp/Retrofit |
| MainActivity.java | 날짜 포맷 유틸 메서드 추출 |
| ShipmentActivity.java | Fragment/ViewModel 분할 |

---

## 변경 이력

| 날짜 | 파일 | 작업 내용 |
|-----|------|----------|
| 2026-01-06 | MainActivity.java | 미사용 변수 `chkProdShip` 및 관련 할당문 삭제 |
| 2026-01-06 | MainActivity.java | 주석 처리된 코드 9개소 삭제 (AlertDialog, chkProdShip 검증 등) |

---

*문서 작성일: 2026-01-06*
*최종 수정일: 2026-01-06*
