# 비정량(searchType=4) 출하 조회 → 계근 → 서버 전송 → ERP 확인 전체 흐름

**작성일**: 2026-04-13
**현재 작업 범위**: search_production_nonfixed.jsp 조회 → insert_goods_wet_new.jsp INSERT → ERP 데이터 확인
**대상**: searchType = 4 (비정량) 전용

**원칙**: 회사코드 및 하드코딩 변경을 제외한 나머지는 원본과 100% 동일해야 한다. (CLAUDE.md)

---

## 1. 전체 흐름도

```
[1단계] 출하대상 조회
  MainActivity.java:258 → downloadShipmentList("4", "비정량출하대상받기")
  PDA (ProgressDlgShipSearch.java:151-153, searchType=4)
    → search_production_nonfixed.jsp:36-130 (MSSQL 15개 테이블 JOIN, 마트사구분='7')
    → 응답 파싱 (ProgressDlgShipSearch.java:194-289, temp[0]~temp[30])
    → DBHandler.refreshShipmentList() (ProgressDlgShipSearch.java:350)
    → TB_SHIPMENT INSERT (로컬 SQLite)

[1-1단계] 바코드 정보 조회 (1단계 완료 후 자동 실행)
  PDA (ProgressDlgBarcodeSearch.java:43-44, selectqueryCodeListForNonFixed)
    → search_barcode_info_nonfixed.jsp:35-61 (B_ITEM 테이블, ITEM_CODE 기준)
    → ProgressDlgBarcodeSearch.java:79-80 → URL_SEARCH_BARCODE_INFO_NONFIXED 호출
    → 응답 파싱 (ProgressDlgBarcodeSearch.java:95-134, temp[0]~temp[23])
    → DBHandler.insertqueryBarcodeInfo() (ProgressDlgBarcodeSearch.java:137)
    → TB_BARCODE_INFO INSERT (로컬 SQLite)
    ※ 바코드 파싱 규칙이 없으면 2단계 계근 불가

[2단계] 바코드 스캔 → 계근
  PDA (BixolonShipmentActivity.java)
    → 1차 스캔 (L1124-1126) → find_PackerProduct() (L1714) → find_work_info() (L1760)
      → TB_BARCODE_INFO에서 ITEM_CODE 매칭, 중복확인 제외 (L1154-1155)
    → 2차 스캔 (L1312) → ITEM_TYPE='HW' 중량 추출
    → wet_data_insert() (L1480→L1540) → DBHandler.insertqueryGoodsWet()
      → TB_GOODS_WET INSERT (로컬 SQLite, SAVE_TYPE='F')
    → 라벨 출력 (L1667-1669) → labelPrintHelper.setPrinting()

[3단계] 서버 전송
  PDA (BixolonShipmentActivity.java, ProgressDlgShipmentSend 내부)
    → TB_GOODS_WET에서 SAV
    E_TYPE='F' SELECT
    → packet 조립 (L2663-2684, ::구분자, 15개 필드, ##행구분)
    → insert_goods_wet_new.jsp:53-92 호출 (L2709-2711, Common.URL_INSERT_GOODS_WET_NEW)
    → W_GOODS_WET INSERT (Oracle)
    → 성공 시 SAVE_TYPE='F'→'Y' UPDATE (L2733)

[4단계] ERP 데이터 확인
  PDA (ProgressDlgGoodsWetSearch.java:76)
    → search_goods_wet.jsp:50-65 호출 (Common.URL_SEARCH_GOODS_WET)
    → SM_출고계근 SELECT (13개 컬럼)
    → 결과 표시
```

---

## 2. 1단계: 출하대상 조회 (search_production_nonfixed.jsp)

### 2.1 PDA 호출 코드

**파일**: `ProgressDlgShipSearch.java:108-109, 150-154`

```java
// WHERE 조건 구성 (공통, 108-109행)
String data = " AND D.회사코드 = '" + Common.selectCompanyCode + "'";
data += " AND D.출고일자 = '" + Common.selectDay + "'";
// ★ 비정량: 창고코드 조건 없음 (이마트는 118-120행에서 추가)

// searchType=4 (비정량) 분기 (150-154행)
}else if(Common.searchType.equals("4")){
    receiveData = HttpHelper.getInstance().sendDataDb(
        data, "inno", "search_shipment", Common.URL_SEARCH_PRODUCTION_NONFIXED);
}
```

### 2.2 search_production_nonfixed.jsp 쿼리 구조

**파일**: `webapps/ROOT/inno/search_production_nonfixed.jsp:36-130`

**JOIN하는 MSSQL 테이블 (15개):**

| 순서 | 별칭 | 테이블 | JOIN 조건 |
|:----:|:----:|--------|----------|
| 1 | D | SM_출고상세 | 메인 테이블 |
| 2 | H | SM_출고머리 | 회사코드, 출고사업장, 출고일자, 출고일련번호 |
| 3 | I | CO_품목코드 | 회사코드, 출고품목코드 |
| 4 | SH | SM_수주머리 | 회사코드, 수주사업장, 수주일자, 수주일련번호 |
| 5 | SD | SM_수주상세 | 회사코드, 수주사업장, 수주일자, 수주일련번호, 순번 |
| 6 | ME | SM_마트사발주이마트 | 마트사SEQ |
| 7 | B | CO_거래처MASTER | 회사코드, 점포코드, 마트사구분='7' |
| 8 | G | CO_거래처MASTER | 회사코드, 거래처코드 |
| 9 | M1 | CO_매출처품목코드매핑 | LEFT JOIN - 회사코드, 품목코드, 거래처코드 |
| 10 | G2 | CO_거래처MASTER | LEFT JOIN - 회사코드, 계층코드(좌5자리) |
| 11 | M2 | CO_매출처품목코드매핑 | LEFT JOIN - 회사코드, 품목코드, 거래처코드 |
| 12 | L | SM_출고LOT | 출고상세SEQ |
| 13 | V | 월품목별재고화일_LOT별_VIEW | LEFT JOIN - 회사코드, 사업장, 창고코드, 품목코드, LOTNO |
| 14 | C | CO_각종소분류코드 | LEFT JOIN - 대분류='043', 제품용도 |
| 15 | C1 | CO_각종소분류코드 | LEFT JOIN - 대분류='Q14', 원산지 |

**WHERE 조건 (비정량 전용):**

```sql
WHERE H.마트사구분 = '7'                                     -- 이마트
  AND D.출고수량 > 0                                         -- 출고수량 있는 것만
  AND COALESCE(M1.타입구분, M2.타입구분) IN ('J', 'B')       -- ★ 이마트 'W' → 비정량 IN('J','B')
  AND COALESCE(M1.바코드타입, M2.바코드타입) = 'M8'           -- ★ 비정량 M8 조건 추가
  AND D.회사코드 = ?                                         -- Common.selectCompanyCode
  AND D.출고일자 = ?                                         -- Common.selectDay
ORDER BY GI_D_ID ASC
```

### 2.3 SELECT 컬럼 및 out.println 출력 순서 (비정량: 31개, JSP L141-172)

| Index | SELECT 별칭 | 원본 컬럼 | 테이블 | PDA 필드 | 이마트와의 차이 |
|:-----:|:----------:|---------|:------:|---------|:--------:|
| 0 | GI_D_ID | D.SEQ | SM_출고상세 | GI_D_ID | - |
| 1 | ITEM_CODE | I.품목코드 | CO_품목코드 | ITEM_CODE | - |
| 2 | ITEM_NAME | I.품목명 | CO_품목코드 | ITEM_NAME | - |
| 3 | EMARTITEM_CODE | ME.상품코드 | SM_마트사발주이마트 | EMARTITEM_CODE | - |
| 4 | EMARTITEM | ME.상품명 | SM_마트사발주이마트 | EMARTITEM | - |
| 5 | GI_REQ_PKG | L.박스수량 | SM_출고LOT | GI_REQ_PKG | - |
| 6 | GI_REQ_QTY | L.중량 | SM_출고LOT | GI_REQ_QTY | - |
| 7 | GI_REQ_DATE | D.출고일자 | SM_출고상세 | GI_REQ_DATE | - |
| 8 | BL_NO | COALESCE(V.BLNO, V.이력번호) | 월품목별재고화일_LOT별_VIEW | BL_NO | - |
| 9 | BRAND_CODE | '' (빈값) | 상수 | BRAND_CODE | - |
| 10 | CLIENT_CODE | ME.점포코드 | SM_마트사발주이마트 | CLIENT_CODE | - |
| 11 | CLIENTNAME | ME.점포명 | SM_마트사발주이마트 | CLIENTNAME | - |
| 12 | CENTERNAME | B.상호 | CO_거래처MASTER | CENTERNAME | - |
| 13 | ITEM_SPEC | I.규격 | CO_품목코드 | ITEM_SPEC | - |
| 14 | CT_CODE | I.원산지 | CO_품목코드 | CT_CODE | - |
| 15 | IMPORT_ID_NO | V.이력번호 | 월품목별재고화일_LOT별_VIEW | IMPORT_ID_NO | - |
| 16 | PACKER_CODE | **'0000'** | **상수** | PACKER_CODE | **★ 이마트: I.패커코드** |
| 17 | PACKER_PRODUCT_CODE | **I.품목코드** | **CO_품목코드** | PACKER_PRODUCT_CODE | **★ 이마트: I.PPCODE** |
| 18 | BARCODE_TYPE | COALESCE(M1.바코드타입, M2.바코드타입) | CO_매출처품목코드매핑 | BARCODE_TYPE | - |
| 19 | ITEM_TYPE | **'HW'** | **상수** | ITEM_TYPE | **★ 이마트: COALESCE(타입구분)** |
| 20 | PACKWEIGHT | COALESCE(NULLIF(V.평균중량,0), I.박스중량) | VIEW / CO_품목코드 | PACKWEIGHT | - |
| 21 | BARCODEGOODS | **I.품목코드** | **CO_품목코드** | BARCODEGOODS | **★ 이마트: I.상품바코드** |
| 22 | STORE_IN_DATE | SD.납기일자 | SM_수주상세 | STORE_IN_DATE | - |
| 23 | EMARTLOGIS_CODE | COALESCE(M1.물류코드, M2.물류코드) | CO_매출처품목코드매핑 | EMARTLOGIS_CODE | - |
| 24 | WH_AREA | B.창고구역 | CO_거래처MASTER | WH_AREA | - |
| 25 | USE_NAME | C.명칭 | CO_각종소분류코드 | USE_NAME | - |
| 26 | USE_CODE | I.제품용도 | CO_품목코드 | USE_CODE | - |
| 27 | CT_NAME | C1.명칭 | CO_각종소분류코드 | CT_NAME | - |
| 28 | STORE_CODE | ME.점포코드 | SM_마트사발주이마트 | STORE_CODE | - |
| 29 | EMART_PLANT_CODE | '' (빈값) | 상수 | EMART_PLANT_CODE | - |
| 30 | GI_L_ID | L.SEQ | SM_출고LOT | GI_L_ID | **★ 이마트: 없음(30개)** |

### 2.4 PDA 파싱 (ProgressDlgShipSearch.java:194-289)

**응답 형식**: `컬럼1::컬럼2::...::컬럼N;;` (row는 `;;`구분, column은 `::`구분)

```java
// L194: row 단위 분리
String[] result = receiveData.split(";;");
for (String s : result) {                    // L205
    temp = s.split("::", -1);                // L207
    si = new Shipments_Info();               // L208
    si.setGI_D_ID(temp[0]);                  // L211
    si.setITEM_CODE(temp[1]);                // L212
    si.setITEM_NAME(temp[2]);                // L213
    si.setEMARTITEM_CODE(temp[3]);           // L214
    si.setEMARTITEM(temp[4]);                // L215
    si.setGI_REQ_PKG(temp[5]);               // L216 (int)Double.parseDouble 처리
    si.setGI_REQ_QTY(temp[6]);               // L244 소수점 4자리 이상이면 1자리로 절사 (L220-241)
    si.setGI_REQ_DATE(temp[7]);              // L245
    si.setBL_NO(temp[8]);                    // L246
    si.setBRAND_CODE(temp[9]);               // L247
    si.setCLIENT_CODE(temp[10]);             // L248
    si.setCLIENTNAME(temp[11]);              // L249
    si.setCENTERNAME(temp[12]);              // L250
    si.setITEM_SPEC(temp[13]);               // L251
    si.setCT_CODE(temp[14]);                 // L252
    si.setIMPORT_ID_NO(temp[15]);            // L253
    si.setPACKER_CODE(temp[16]);             // L254
    si.setPACKER_PRODUCT_CODE(temp[17]);     // L255
    si.setBARCODE_TYPE(temp[18]);            // L256
    si.setITEM_TYPE(temp[19]);               // L257
    si.setPACKWEIGHT(temp[20]);              // L258
    si.setBARCODEGOODS(temp[21]);            // L259
    si.setSTORE_IN_DATE(temp[22]);           // L260
    si.setEMARTLOGIS_CODE(temp[23]);         // L261

    // 이마트(0), 비정량(4) 공유: temp[24]~temp[30] (L265-272)
    if(Common.searchType.equals("0") || Common.searchType.equals("4")) {
        si.setWH_AREA(temp[24]);             // L266
        si.setUSE_NAME(temp[25]);            // L267
        si.setUSE_CODE(temp[26]);            // L268
        si.setCT_NAME(temp[27]);             // L269
        si.setSTORE_CODE(temp[28]);          // L270
        si.setEMART_PLANT_CODE(temp[29]);    // L271
        si.setGI_L_ID(temp[30]);             // L272 ★ 이마트도 31개, 비정량과 동일
    }

    si.setSAVE_TYPE("F");                    // L289
    // L292: list_si_info.add(si) 후 L350에서 DBHandler.refreshShipmentList() 호출
}
```

---

## 3. 1-1단계: 바코드 정보 조회 (search_barcode_info_nonfixed.jsp)

1단계 완료 후 **자동 실행**됨 (ProgressDlgShipSearch 내부에서 ProgressDlgBarcodeSearch 호출)

### 3.1 PDA 호출 코드

**파일**: `ProgressDlgBarcodeSearch.java:43-80`

```java
// L43-44: ★ 비정량: PPCODE가 아닌 ITEM_CODE로 코드 리스트 조회
if(Common.searchType.equals("4") || Common.searchType.equals("5")){
    list_code_info = DBHandler.selectqueryCodeListForNonFixed(mContext);
    // → DBHandler.java:586 TB_SHIPMENT에서 SELECT DISTINCT ITEM_CODE, ITEM_NAME
}

// L49-57: WHERE 조건 구성
String data = " WHERE ";
for (int i = 0; i < list_code_info.size(); i++) {
    if(Common.searchType.equals("4") || Common.searchType.equals("5")){  // L51
        // ★ 비정량: SBI.ITEM_CODE 사용 (이마트: SBI.PPCODE, L60)
        data = data + "SBI.ITEM_CODE = '" + list_code_info.get(i)[0].toString() + "'";  // L54
    }
    if (i != list_code_info.size() - 1) data += " OR ";
}

// L79-80: searchType=4 분기 → URL_SEARCH_BARCODE_INFO_NONFIXED 호출
}else if(Common.searchType.equals("4")) {
    receiveData = HttpHelper.getInstance().sendDataDb(
        data, "inno", "search_barcode_info", Common.URL_SEARCH_BARCODE_INFO_NONFIXED);
}

// L132-134: 파싱 시 비정량은 SHELF_LIFE(temp[24]) 제외
// L137: DBHandler.insertqueryBarcodeInfo(mContext, bi) → TB_BARCODE_INFO INSERT
```

### 3.2 search_barcode_info_nonfixed.jsp 쿼리 구조

**파일**: `webapps/ROOT/inno/search_barcode_info_nonfixed.jsp:35-61`

**테이블**: B_ITEM (1개 테이블, JOIN 없음)

```sql
SELECT '이마트용' as PACKER_CLIENT_CODE         -- 고정값
     , sbi.ITEM_CODE as PACKER_PRODUCT_CODE    -- ★ 이마트: PPCODE
     , sbi.ITEM_NAME_KR as PACKER_PRD_NAME
     , sbi.ITEM_CODE as ITEMCODE
     , sbi.ITEM_NAME_KR as ITEM_NAME_KR
     , '0000' as BRAND_CODE                     -- 고정값
     , sbi.ITEM_CODE as BARCODEGOODS            -- ★ 이마트: 상품바코드
     , 'KG' as BASEUNIT                         -- 고정값
     , SBI.ZEROPOINT
     , SBI.PACKER_PRD_CODE_FROM
     , SBI.PACKER_PRD_CODE_TO
     , SBI.BARCODEGOODS_FROM
     , SBI.BARCODEGOODS_TO
     , SBI.WEIGHT_FROM
     , SBI.WEIGHT_TO
     , SBI.MAKINGDATE_FROM
     , SBI.MAKINGDATE_TO
     , SBI.BOXSERIAL_FROM
     , SBI.BOXSERIAL_TO
     , SBI.STATUS
     , SBI.REG_ID
     , SBI.REG_DATE
     , SBI.REG_TIME
     , '0000' AS memo                           -- 고정값
FROM B_ITEM sbi
WHERE [ITEM_CODE 조건]
ORDER BY ITEMCODE ASC
```

### 3.3 out.println 출력 순서 (24개 컬럼, JSP L73-80)

| Index | SELECT 별칭 | 원본 컬럼 | PDA 필드 |
|:-----:|:----------:|---------|---------|
| 0 | PACKER_CLIENT_CODE | '이마트용' (고정) | PACKER_CLIENT_CODE |
| 1 | PACKER_PRODUCT_CODE | sbi.ITEM_CODE | PACKER_PRODUCT_CODE |
| 2 | PACKER_PRD_NAME | sbi.ITEM_NAME_KR | PACKER_PRD_NAME |
| 3 | ITEMCODE | sbi.ITEM_CODE | ITEMCODE |
| 4 | ITEM_NAME_KR | sbi.ITEM_NAME_KR | ITEM_NAME_KR |
| 5 | BRAND_CODE | '0000' (고정) | BRAND_CODE |
| 6 | BARCODEGOODS | sbi.ITEM_CODE | BARCODEGOODS |
| 7 | BASEUNIT | 'KG' (고정) | BASEUNIT |
| 8 | ZEROPOINT | SBI.ZEROPOINT | ZEROPOINT |
| 9 | PACKER_PRD_CODE_FROM | SBI.PACKER_PRD_CODE_FROM | PACKER_PRD_CODE_FROM |
| 10 | PACKER_PRD_CODE_TO | SBI.PACKER_PRD_CODE_TO | PACKER_PRD_CODE_TO |
| 11 | BARCODEGOODS_FROM | SBI.BARCODEGOODS_FROM | BARCODEGOODS_FROM |
| 12 | BARCODEGOODS_TO | SBI.BARCODEGOODS_TO | BARCODEGOODS_TO |
| 13 | WEIGHT_FROM | SBI.WEIGHT_FROM | WEIGHT_FROM |
| 14 | WEIGHT_TO | SBI.WEIGHT_TO | WEIGHT_TO |
| 15 | MAKINGDATE_FROM | SBI.MAKINGDATE_FROM | MAKINGDATE_FROM |
| 16 | MAKINGDATE_TO | SBI.MAKINGDATE_TO | MAKINGDATE_TO |
| 17 | BOXSERIAL_FROM | SBI.BOXSERIAL_FROM | BOXSERIAL_FROM |
| 18 | BOXSERIAL_TO | SBI.BOXSERIAL_TO | BOXSERIAL_TO |
| 19 | STATUS | SBI.STATUS | STATUS |
| 20 | REG_ID | SBI.REG_ID | REG_ID |
| 21 | REG_DATE | SBI.REG_DATE | REG_DATE |
| 22 | REG_TIME | SBI.REG_TIME | REG_TIME |
| 23 | memo | '0000' (고정) | memo |

**★ 이마트와의 차이**: SHELF_LIFE(temp[24]) 미포함. 비정량은 temp[0]~temp[23] 24개만 파싱 (L132-134)

### 3.4 필수 데이터

| 필드 | 용도 | 없으면 |
|------|------|--------|
| PACKER_PRODUCT_CODE (ITEM_CODE) | 1차 스캔 시 상품 매칭 | 상품 인식 불가 |
| BARCODEGOODS_FROM / TO | 바코드에서 상품코드 추출 위치 | 바코드 파싱 불가 |
| WEIGHT_FROM / TO | 바코드에서 중량 추출 위치 | 중량 추출 불가 |
| BASEUNIT | KG 고정 (비정량) | 중량 환산 불가 |
| ZEROPOINT | 소수점 자리수 | 중량 계산 오류 |

**★ 이마트와의 차이**: 이마트는 CO_품목코드+CO_거래처MASTER 등 다중 JOIN, 비정량은 B_ITEM 단일 테이블

---

## 4. 2단계: 바코드 스캔 → 계근 → 로컬 DB 저장

### 4.1 1차 스캔 (상품 바코드, work_flag=1)

**파일**: `BixolonShipmentActivity.java`

```java
// L1124-1126: 상품 바코드 스캔
if (work_flag == 1) {                        // L1124
    find_ppcodetemp = find_PackerProduct(msg); // L1126 → L1714로 이동
```

**find_PackerProduct() (L1714-1729) → find_work_info() (L1760-1825)**:
- L1760: TB_BARCODE_INFO에서 BARCODEGOODS 매칭 (BARCODEGOODS_FROM~TO 위치로 추출)
- **★ 비정량 전용 (L1803-1815)**: searchType=4일 때 BARCODEGOODS 일치 여부와 관계없이 바로 work_item_bi_info에 설정

### 4.2 중복확인 제외

**파일**: `BixolonShipmentActivity.java:1154-1156, 1275-1277`

```java
// L1154-1155: 1차 스캔 중복확인 제외
if(Common.searchType.equals(SEARCH_TYPE_NONFIXED)        // L1154
   || Common.searchType.equals(SEARCH_TYPE_HOMEPLUS_NONFIXED)){
    dup = false;                                          // L1155
}

// L1275-1276: 2차 스캔(BL) 중복확인 제외
if (Common.searchType.equals(SEARCH_TYPE_NONFIXED)       // L1275
    || Common.searchType.equals(SEARCH_TYPE_HOMEPLUS_NONFIXED)) {
    dup = false;                                          // L1276
}
```

### 4.3 2차 스캔 (BL번호, work_flag=0) — ITEM_TYPE='HW' 처리

**파일**: `BixolonShipmentActivity.java:1312-1352`

```java
// L1312: ITEM_TYPE_W('W', L197) 또는 ITEM_TYPE_HW('HW', L198) 동일 처리
if (arSM.get(current_work_position).getITEM_TYPE().equals(ITEM_TYPE_W) ||  // L1312
    arSM.get(current_work_position).getITEM_TYPE().equals(ITEM_TYPE_HW)) {
    // L1319: 바코드에서 중량 추출 (WEIGHT_FROM~WEIGHT_TO 위치)
    item_weight = work_item_fullbarcode.substring(
        Integer.parseInt(work_item_bi_info.getWEIGHT_FROM()) - 1, 
        Integer.parseInt(work_item_bi_info.getWEIGHT_TO()));

    // L1323: ZeroPoint 적용
    item_pow = Math.pow(10, Integer.parseInt(work_item_bi_info.getZEROPOINT()));
    item_weight_double = Double.parseDouble(item_weight) / item_pow;

    // L1335: LB→KG 환산 (필요시)
    if ("LB".equals(work_item_bi_info.getBASEUNIT())) {
        item_weight_double = item_weight_double * 0.453592;
    }

    // L1345: 소수점 1자리 절사
    item_weight_double = Math.floor(item_weight_double * 10) / 10.0;
}

// L1480: wet_data_insert() 호출
wet_data_insert(item_weight_str, item_weight_double, item_making_date, item_box_serial);
```

### 4.4 wet_data_insert() — 로컬 DB 저장

**파일**: `BixolonShipmentActivity.java:1540-1589`

```java
// L1540: wet_data_insert() 메서드 시작
Goodswets_Info gi = new Goodswets_Info();
gi.setGI_D_ID(arSM.get(pos).getGI_D_ID());       // L1553
gi.setGI_L_ID(arSM.get(pos).getGI_L_ID());       // L1554 ★ GI_L_ID 설정
gi.setWEIGHT(weight_str);                         // L1555
gi.setWEIGHT_UNIT(work_item_bi_info.getBASEUNIT());  // L1556
gi.setPACKER_PRODUCT_CODE(arSM.get(pos).getPACKER_PRODUCT_CODE());  // L1557
gi.setBARCODE(work_item_fullbarcode);              // L1558
gi.setPACKER_CLIENT_CODE(work_item_bi_info.getPACKER_CLIENT_CODE());  // L1559
gi.setMAKINGDATE(making_date);                     // L1560
gi.setBOXSERIAL(box_serial);                      // L1561
gi.setBOX_CNT(String.valueOf(arSM.get(pos).getPACKING_QTY() + 1));  // L1562
gi.setEMARTITEM_CODE(arSM.get(pos).getEMARTITEM_CODE());  // L1563
gi.setEMARTITEM(arSM.get(pos).getEMARTITEM());    // L1564
gi.setITEM_CODE(arSM.get(pos).getITEM_CODE());    // L1565
gi.setBRAND_CODE(arSM.get(pos).getBRAND_CODE());   // L1566
gi.setREG_ID(Common.REG_ID);                      // L1567
gi.setSAVE_TYPE("F");                              // L1568
gi.setDUPLICATE("F");                              // L1569

// L1589: ★ 비정량(4)은 기본 insertqueryGoodsWet() 사용 (이마트와 동일)
DBHandler.insertqueryGoodsWet(this, gi);
```

### 4.5 중량 표시 처리 차이

**파일**: `BixolonShipmentActivity.java:1595-1601`

```java
if(Common.searchType.equals(SEARCH_TYPE_EMART)) {
    // 이마트: 소수점 1자리
    weight_double = Math.floor(weight_double * 10) / 10.0;
    temp_weight = String.format("%.1f", weight_double);
}else{
    // ★ 비정량: 그대로 입력
    temp_weight = Double.toString(weight_double);
}
```

### 4.6 라벨 인쇄 분기

**파일**: `BixolonShipmentActivity.java:1661-1669`

```java
}else if(Common.searchType.equals(SEARCH_TYPE_NONFIXED)){  // L1667
    // ★ 비정량: 이마트와 동일한 setPrinting() 메서드 호출
    labelPrintHelper.setPrinting(weight_double,             // L1669
        arSM.get(current_work_position), false, making_date,
        work_item_bi_info, arSM.get(current_work_position),
        Common.searchType, printerCallback);
}
```

---

## 5. 3단계: 서버 전송 (insert_goods_wet_new.jsp)

### 5.1 PDA packet 조립 (BixolonShipmentActivity.java:2663-2684)

```java
// L2663: 비정량(4)은 생산(1)/도매(3)/홈플비정량(5)/생산라벨(7)과 동일 분기
}else if(Common.searchType.equals(SEARCH_TYPE_PRODUCTION)      // L2663
      || Common.searchType.equals(SEARCH_TYPE_WHOLESALE) 
      || Common.searchType.equals(SEARCH_TYPE_NONFIXED) 
      || Common.searchType.equals(SEARCH_TYPE_HOMEPLUS_NONFIXED) 
      || Common.searchType.equals(SEARCH_TYPE_PRODUCTION_LABEL)){

    String packet = "";
    for (int i = 0; i < list_send_info.size(); i++) {
        if (list_send_info.get(i).getSAVE_TYPE().equals("F")) {   // L2669
            packet += list_send_info.get(i).getGI_D_ID() + "::";              // L2671 [0]
            packet += list_send_info.get(i).getWEIGHT() + "::";              // L2672 [1]
            packet += list_send_info.get(i).getWEIGHT_UNIT() + "::";         // L2673 [2]
            packet += list_send_info.get(i).getPACKER_PRODUCT_CODE() + "::"; // L2674 [3]
            packet += list_send_info.get(i).getBARCODE() + "::";             // L2675 [4]
            packet += list_send_info.get(i).getPACKER_CLIENT_CODE() + "::";  // L2676 [5]
            packet += list_send_info.get(i).getMAKINGDATE() + "::";          // L2677 [6]
            packet += list_send_info.get(i).getBOXSERIAL() + "::";           // L2678 [7]
            packet += list_send_info.get(i).getBOX_CNT() + "::";            // L2679 [8]
            packet += list_send_info.get(i).getREG_ID() + "::";             // L2680 [9]
            packet += Common.selectCompanyCode + "::";                        // L2681 [10]
            packet += list_send_info.get(i).getBRAND_CODE() + "::";          // L2682 [11]
            packet += list_send_info.get(i).getCLIENT_TYPE() + "::";         // L2683 [12]
            packet += list_send_info.get(i).getBOX_ORDER() + "::"            // L2684 [13]
                   + list_send_info.get(i).getGI_L_ID() +"##";               // L2684 [14] ★ GI_L_ID + "##" 행구분
        }
    }
}
```

**★ 이마트와의 차이**:
- 이마트: `";;"` 행구분, 14개 필드
- 비정량: `"##"` 행구분, 15개 필드 (GI_L_ID 추가)

### 5.2 전송 URL (비정량)

**파일**: `BixolonShipmentActivity.java:2709-2711`

```java
// ★ 비정량(4): URL_INSERT_GOODS_WET_NEW 호출 (이마트: URL_INSERT_GOODS_WET)
}else if(Common.searchType.equals(SEARCH_TYPE_PRODUCTION) 
      || Common.searchType.equals(SEARCH_TYPE_NONFIXED)
      || Common.searchType.equals(SEARCH_TYPE_HOMEPLUS_NONFIXED)
      || Common.searchType.equals(SEARCH_TYPE_PRODUCTION_LABEL)){
    result = HttpHelper.getInstance().sendDataDb(
        packet, "inno", "goodswet_insert", Common.URL_INSERT_GOODS_WET_NEW);
}
```

- **URL**: `http://192.168.0.28:4040/inno/insert_goods_wet_new.jsp`

### 5.3 insert_goods_wet_new.jsp INSERT 쿼리

**파일**: `webapps/ROOT/inno/insert_goods_wet_new.jsp:53-67`

```sql
INSERT INTO W_GOODS_WET(
    GOODS_WET_ID, GI_D_ID, WEIGHT, WEIGHT_UNIT, PACKER_PRODUCT_CODE,
    BARCODE, PACKER_CLIENT_CODE, MAKINGDATE, BOXSERIAL, BOX_CNT,
    REG_ID, REG_DATE, REG_TIME
) VALUES (
    W_GOODS_WET_SEQ.NEXTVAL, ?,?,?,?,?,?,?,?,?,?,?,?
)
```

**★ 주의**: 이 JSP는 **Oracle** `W_GOODS_WET` 테이블에 INSERT (MSSQL `SM_출고계근`이 아님)

### 5.4 splitData → PreparedStatement 매핑

| pstmt | splitData | W_GOODS_WET 컬럼 | 타입 | 비고 |
|:-----:|:---------:|:---------------:|:----:|------|
| - | - | GOODS_WET_ID | SEQ | W_GOODS_WET_SEQ.NEXTVAL |
| 1 | splitData[0] | GI_D_ID | INT | 출고상세SEQ |
| 2 | splitData[1] | WEIGHT | DOUBLE | `(Double * 100) / 100.0` |
| 3 | splitData[2] | WEIGHT_UNIT | STRING | KG |
| 4 | splitData[3] | PACKER_PRODUCT_CODE | STRING | 품목코드 |
| 5 | splitData[4] | BARCODE | STRING | 계근바코드 |
| 6 | splitData[5] | PACKER_CLIENT_CODE | STRING | '이마트용' |
| 7 | splitData[6] | MAKINGDATE | STRING | 제조일자 |
| 8 | splitData[7] | BOXSERIAL | STRING | 박스시리얼 |
| 9 | splitData[8] | BOX_CNT | INT | 계근순번 |
| 10 | splitData[9] | REG_ID | STRING | 등록사원 |
| 11 | - | REG_DATE | STRING | 서버 자동 (dateStr) |
| 12 | - | REG_TIME | STRING | 서버 자동 (timeStr) |

**★ 이마트와의 차이**:
- 이마트 JSP: SM_출고계근 INSERT (MSSQL), 회사코드 포함
- 비정량 JSP: W_GOODS_WET INSERT (Oracle), 회사코드 미포함, splitData[10]~[14] 미사용

### 5.5 전송 결과 처리

**성공 시** (BixolonShipmentActivity.java:2731-2744):
```java
// L2733: 전송 성공 시 SAVE_TYPE 업데이트
boolean bool = DBHandler.updatequeryGoodsWet(mContext,
    list_send_info.get(i).getGI_D_ID(),
    list_send_info.get(i).getBARCODE(),
    list_send_info.get(i).getBOX_CNT(),
    list_send_info.get(i).getGI_L_ID());
// L2744: arSM.get(j).setSAVE_TYPE("Y") → TB_GOODS_WET의 SAVE_TYPE: 'F' → 'Y'
```

**실패 시**:
```java
// 전송 실패 시 SAVE_TYPE='F' 유지 (재전송 가능)
// JSP가 "f" 반환하면 out.println("f") → result = "f"
```

---

## 6. 4단계: ERP 데이터 확인 (search_goods_wet.jsp)

**파일**: `ProgressDlgGoodsWetSearch.java:76`

```java
// ★ 비정량도 이마트와 동일 JSP 사용 (searchType 구분 없음)
receiveData = HttpHelper.getInstance().sendDataDb(
    data, "inno", "search_goods_wet", Common.URL_SEARCH_GOODS_WET);
```

**파일**: `webapps/ROOT/inno/search_goods_wet.jsp:50-65`

```sql
SELECT 출고상세SEQ AS GI_D_ID
     , 계근중량 AS WEIGHT
     , 계근중량단위 AS WEIGHT_UNIT
     , ppCode AS PACKER_PRODUCT_CODE
     , 계근바코드 AS BARCODE
     , 패커코드 AS PACKER_CLIENT_CODE
     , 계근순번 AS BOX_CNT
     , 등록사원 AS REG_ID
     , 등록일자 AS REG_DATE
     , 등록시간 AS REG_TIME
     , 제조일자 AS MAKINGDATE
     , 박스시리얼 AS BOXSERIAL
     , '' AS BOX_ORDER
FROM SM_출고계근
WHERE [qry_where]
ORDER BY GI_D_ID ASC
```

**응답 형식**: `GI_D_ID::WEIGHT::...::BOX_ORDER;;` (13개 컬럼, `::`구분, `;;`행구분)

---

## 7. 핵심 파일 경로

| 단계 | 파일 | 역할 |
|:----:|------|------|
| - | MainActivity.java:258 | downloadShipmentList("4", "비정량출하대상받기") 호출 |
| - | Common.java:36 | URL_SEARCH_PRODUCTION_NONFIXED 상수 정의 |
| - | Common.java:27 | URL_SEARCH_BARCODE_INFO_NONFIXED 상수 정의 |
| - | Common.java:30 | URL_INSERT_GOODS_WET_NEW 상수 정의 |
| - | Common.java:28 | URL_SEARCH_GOODS_WET 상수 정의 |
| 1 | ProgressDlgShipSearch.java:151-153 | searchType=4 분기 → URL_SEARCH_PRODUCTION_NONFIXED 호출 |
| 1 | search_production_nonfixed.jsp:36-130 | MSSQL 출하대상 조회 (15개 JOIN, 타입구분 IN('J','B'), 바코드타입='M8') |
| 1 | search_production_nonfixed.jsp:141-172 | out.println 31개 컬럼 출력 |
| 1 | ProgressDlgShipSearch.java:211-272 | 서버 응답 파싱 (temp[0]~[30]) |
| 1 | ProgressDlgShipSearch.java:350 | DBHandler.refreshShipmentList() → TB_SHIPMENT 동기화 |
| 1-1 | ProgressDlgBarcodeSearch.java:43-44 | selectqueryCodeListForNonFixed() (ITEM_CODE 기준) |
| 1-1 | ProgressDlgBarcodeSearch.java:79-80 | searchType=4 → URL_SEARCH_BARCODE_INFO_NONFIXED 호출 |
| 1-1 | search_barcode_info_nonfixed.jsp:35-61 | B_ITEM 테이블에서 바코드 파싱 규칙 조회 |
| 1-1 | search_barcode_info_nonfixed.jsp:73-80 | out.println 24개 컬럼 출력 |
| 1-1 | ProgressDlgBarcodeSearch.java:137 | DBHandler.insertqueryBarcodeInfo() → TB_BARCODE_INFO INSERT |
| 2 | BixolonShipmentActivity.java:1124-1126 | 1차 스캔 → find_PackerProduct(msg) 호출 |
| 2 | BixolonShipmentActivity.java:1714-1729 | find_PackerProduct() → find_work_info() 호출 |
| 2 | BixolonShipmentActivity.java:1760-1825 | find_work_info() — 비정량 전용 매칭 (L1803) |
| 2 | BixolonShipmentActivity.java:1154-1155 | 1차 스캔 중복확인 제외 |
| 2 | BixolonShipmentActivity.java:1275-1276 | 2차 스캔 중복확인 제외 |
| 2 | BixolonShipmentActivity.java:1312-1352 | 2차 스캔 — ITEM_TYPE='HW' (L198) 중량 추출 |
| 2 | BixolonShipmentActivity.java:1480 | wet_data_insert() 호출 |
| 2 | BixolonShipmentActivity.java:1540-1589 | wet_data_insert() → DBHandler.insertqueryGoodsWet() |
| 2 | BixolonShipmentActivity.java:1667-1669 | 라벨 인쇄 — setPrinting() (비정량 분기) |
| 3 | BixolonShipmentActivity.java:2663-2684 | packet 조립 (15개 필드, "##" 행구분) |
| 3 | BixolonShipmentActivity.java:2709-2711 | URL_INSERT_GOODS_WET_NEW 전송 |
| 3 | insert_goods_wet_new.jsp:53-67 | W_GOODS_WET INSERT 쿼리 정의 |
| 3 | insert_goods_wet_new.jsp:76-87 | PreparedStatement 파라미터 매핑 |
| 3 | BixolonShipmentActivity.java:2733 | 전송 성공 시 updatequeryGoodsWet() |
| 3 | BixolonShipmentActivity.java:2744 | SAVE_TYPE='F'→'Y' 업데이트 |
| 4 | ProgressDlgGoodsWetSearch.java:76 | search_goods_wet.jsp 호출 (이마트와 동일) |
| 4 | search_goods_wet.jsp:50-65 | SM_출고계근 SELECT (확인용) |

---

## 8. 데이터 상태 관리

```
TB_GOODS_WET.SAVE_TYPE 상태 흐름:

바코드 스캔 → INSERT (SAVE_TYPE='F')
    ↓
전송 버튼 → SELECT WHERE SAVE_TYPE='F'
    ↓
insert_goods_wet_new.jsp → W_GOODS_WET INSERT (Oracle)
    ↓
성공 → UPDATE SAVE_TYPE='Y'
실패 → SAVE_TYPE='F' 유지 (재전송 가능)
```

---

## 9. 이마트(searchType=0) vs 비정량(searchType=4) 차이 종합

| 항목 | 이마트(0) | 비정량(4) |
|------|---------|---------|
| **출하 조회 JSP** | search_shipment.jsp | search_production_nonfixed.jsp |
| **출하 조회 URL** | URL_SEARCH_SHIPMENT | URL_SEARCH_PRODUCTION_NONFIXED |
| **창고코드 조건** | 추가 가능 | 없음 |
| **WHERE 타입구분** | = 'W' | IN ('J', 'B') |
| **WHERE 바코드타입** | 없음 | = 'M8' |
| **PACKER_CODE** | I.패커코드 | '0000' (고정) |
| **PACKER_PRODUCT_CODE** | I.PPCODE | I.품목코드 |
| **ITEM_TYPE** | COALESCE(M1.타입구분, M2.타입구분) | 'HW' (고정) |
| **BARCODEGOODS** | I.상품바코드 | I.품목코드 |
| **출력 컬럼 수** | 30개 | 31개 (GI_L_ID 추가) |
| **바코드 조회 JSP** | search_barcode_info.jsp | search_barcode_info_nonfixed.jsp |
| **바코드 조회 KEY** | SBI.PPCODE | SBI.ITEM_CODE |
| **바코드 조회 테이블** | 다중 JOIN | B_ITEM 단일 |
| **바코드 파싱 컬럼 수** | 25개 (SHELF_LIFE 포함) | 24개 (SHELF_LIFE 제외) |
| **중복 스캔 확인** | 체크함 | 제외 (같은 바코드 여러 번 가능) |
| **중량 표시** | 소수점 1자리 (String.format) | 그대로 입력 (Double.toString) |
| **전송 JSP** | insert_goods_wet.jsp | insert_goods_wet_new.jsp |
| **전송 URL** | URL_INSERT_GOODS_WET | URL_INSERT_GOODS_WET_NEW |
| **전송 행구분** | ";;" | "##" |
| **전송 필드 수** | 14개 | 15개 (GI_L_ID 추가) |
| **서버 INSERT 대상** | SM_출고계근 (MSSQL) | W_GOODS_WET (Oracle) |
| **ERP 확인 JSP** | search_goods_wet.jsp (동일) | search_goods_wet.jsp (동일) |
| **라벨 인쇄** | setPrinting() | setPrinting() (동일 메서드) |

---

## 10. MSSQL 전환 현황

| 단계 | JSP | 현재 DB | 전환 대상 | 상태 |
|:----:|-----|:------:|:--------:|:----:|
| 1 | search_production_nonfixed.jsp | **MSSQL** (전환 완료) | - | ✅ |
| 1-1 | search_barcode_info_nonfixed.jsp | **Oracle** (B_ITEM) | CO_품목코드 등 MSSQL | ❌ |
| 3 | insert_goods_wet_new.jsp | **Oracle** (W_GOODS_WET) | SM_출고계근 MSSQL | ❌ |
| 4 | search_goods_wet.jsp | **MSSQL** (SM_출고계근) | - | ✅ |

---

## 11. 관련 문서

- `app/doc/소스분석/39_이마트출하_조회부터_계근전송_전체흐름.md` — 이마트(searchType=0) 전체 흐름 (본 문서 원본 형식)
- `app/doc/소스분석/40_비정량_출하계근대상받기_JSP_Java파싱_인덱스분석.md`
- `app/doc/소스분석/41_이마트VIEW_vs_비정량VIEW_비교분석.md`
- `app/doc/소스분석/42_이마트Oracle_MSSQL_JSP_비정량Oracle_3자비교분석.md`
- `app/doc/소스분석/43_이마트(0)_비정량(4)_Java파싱_공유분리_분석.md`
- `app/doc/개발/35_비정량_출하계근대상_JSP_MSSQL전환.md`
- `app/doc/테스트/02_비정량출하계근대상받기.md`
- `app/doc/기능/11_바코드스캔후_전체프로세스.md`
